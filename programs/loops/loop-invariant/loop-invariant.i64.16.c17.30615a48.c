// an invariant multiply inside a 16 iteration long long loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 3ll;
static volatile long long b_in = 5ll;

int main(void) {
    long long a = a_in;
    long long b = b_in;

    long long total = 0;
    for (int i = 0; i < 16; i++) {
        total += a * b + (long long)i;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
