// the same walk over structs of six unsigned int, whose stride is not
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int n_in = 16;
struct wide { unsigned int f[6]; };

int main(void) {
    int n = n_in;
    struct wide a[20];
    for (int i = 0; i < 20; i++) {
        for (int k = 0; k < 6; k++) a[i].f[k] = (unsigned int)(i + k);
    }
    unsigned int total = 0;
    for (int i = 0; i < n; i++) {
        total += a[i].f[0] + a[i].f[3];
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
