// a walk over structs of eight int, whose stride is a power of two
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int n_in = 16;
struct wide { int f[8]; };

int main(void) {
    int n = n_in;
    struct wide a[20];
    for (int i = 0; i < 20; i++) {
        for (int k = 0; k < 8; k++) a[i].f[k] = (int)(i + k);
    }
    int total = 0;
    for (int i = 0; i < n; i++) {
        total += a[i].f[0] + a[i].f[3];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
