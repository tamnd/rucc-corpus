// two unsigned long long arrays walked at once, which want one each
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    unsigned long long a[20], b[20];
    for (int i = 0; i < 20; i++) a[i] = (unsigned long long)(i * 1);
    for (int i = 0; i < 20; i++) b[i] = (unsigned long long)(i * 2);
    unsigned long long total = 0;
    for (int i = 0; i < 16; i++) {
        total += a[i] + b[i];
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
