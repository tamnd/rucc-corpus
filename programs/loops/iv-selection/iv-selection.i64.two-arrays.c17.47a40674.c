// two long long arrays walked at once, which want one each
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    long long a[20], b[20];
    for (int i = 0; i < 20; i++) a[i] = (long long)(i * 1);
    for (int i = 0; i < 20; i++) b[i] = (long long)(i * 2);
    long long total = 0;
    for (int i = 0; i < 16; i++) {
        total += a[i] + b[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
