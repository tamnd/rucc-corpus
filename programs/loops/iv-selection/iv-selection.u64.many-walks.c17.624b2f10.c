// six unsigned long long walks at once, which is more than the registers
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    unsigned long long a1[20];
    for (int i = 0; i < 20; i++) a1[i] = (unsigned long long)(i * 1);
    unsigned long long a2[20];
    for (int i = 0; i < 20; i++) a2[i] = (unsigned long long)(i * 2);
    unsigned long long a3[20];
    for (int i = 0; i < 20; i++) a3[i] = (unsigned long long)(i * 3);
    unsigned long long a4[20];
    for (int i = 0; i < 20; i++) a4[i] = (unsigned long long)(i * 4);
    unsigned long long a5[20];
    for (int i = 0; i < 20; i++) a5[i] = (unsigned long long)(i * 5);
    unsigned long long a6[20];
    for (int i = 0; i < 20; i++) a6[i] = (unsigned long long)(i * 6);
    unsigned long long total = 0;
    for (int i = 0; i < 16; i++) {
        total += a1[i] + a2[i] + a3[i] + a4[i] + a5[i] + a6[i];
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
