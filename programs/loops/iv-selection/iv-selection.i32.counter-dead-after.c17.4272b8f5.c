// a int loop whose counter nothing reads afterwards
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int n_in = 16;

int main(void) {
    int n = n_in;
    int a[20];
    for (int i = 0; i < 20; i++) a[i] = (int)(i * 1);
    int total = 0;
    for (int i = 0; i < n; i++) {
        total += a[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
