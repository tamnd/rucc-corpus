// a 4 element copy over long long, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    long long source[4];
    long long target[4];
    for (int i = 0; i < 4; i++) {
        source[i] = (long long)(i + 1);
    }

    for (int i = 0; i < 4; i++) {
        target[i] = source[i];
    }

    printf("%lld\n", (long long)(target[0]));
    printf("%lld\n", (long long)(target[3]));
    return 0;
}
