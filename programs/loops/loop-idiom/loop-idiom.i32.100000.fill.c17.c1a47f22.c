// a 100000 element fill over int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int source[100000];
    int target[100000];
    for (int i = 0; i < 100000; i++) {
        source[i] = (int)(i + 1);
    }

    for (int i = 0; i < 100000; i++) {
        target[i] = (int)0;
    }

    printf("%lld\n", (long long)(target[0]));
    printf("%lld\n", (long long)(target[99999]));
    return 0;
}
