// a 100000 element copy over long long, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    long long source[100000];
    long long target[100000];
    for (int i = 0; i < 100000; i++) {
        source[i] = (long long)(i + 1);
    }

    for (int i = 0; i < 100000; i++) {
        target[i] = source[i];
    }

    printf("%lld\n", (long long)(target[0]));
    printf("%lld\n", (long long)(target[99999]));
    return 0;
}
