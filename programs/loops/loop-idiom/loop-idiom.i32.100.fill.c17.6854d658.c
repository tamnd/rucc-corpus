// a 100 element fill over int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int source[100];
    int target[100];
    for (int i = 0; i < 100; i++) {
        source[i] = (int)(i + 1);
    }

    for (int i = 0; i < 100; i++) {
        target[i] = (int)0;
    }

    printf("%lld\n", (long long)(target[0]));
    printf("%lld\n", (long long)(target[99]));
    return 0;
}
