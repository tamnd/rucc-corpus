// a 4 element fill over int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int source[4];
    int target[4];
    for (int i = 0; i < 4; i++) {
        source[i] = (int)(i + 1);
    }

    for (int i = 0; i < 4; i++) {
        target[i] = (int)0;
    }

    printf("%lld\n", (long long)(target[0]));
    printf("%lld\n", (long long)(target[3]));
    return 0;
}
