// a 16 element fill over int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int source[16];
    int target[16];
    for (int i = 0; i < 16; i++) {
        source[i] = (int)(i + 1);
    }

    for (int i = 0; i < 16; i++) {
        target[i] = (int)0;
    }

    printf("%lld\n", (long long)(target[0]));
    printf("%lld\n", (long long)(target[15]));
    return 0;
}
