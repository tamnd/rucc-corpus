// a 7 element sum over int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int source[7];
    int target[7];
    for (int i = 0; i < 7; i++) {
        source[i] = (int)(i + 1);
    }

    int total = 0;
    for (int i = 0; i < 7; i++) {
        total += source[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
