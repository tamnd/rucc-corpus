// a 4 element fill over unsigned int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    unsigned int source[4];
    unsigned int target[4];
    for (int i = 0; i < 4; i++) {
        source[i] = (unsigned int)(i + 1);
    }

    for (int i = 0; i < 4; i++) {
        target[i] = (unsigned int)0u;
    }

    printf("%llu\n", (unsigned long long)(target[0]));
    printf("%llu\n", (unsigned long long)(target[3]));
    return 0;
}
