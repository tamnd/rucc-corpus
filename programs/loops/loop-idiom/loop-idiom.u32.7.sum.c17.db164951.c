// a 7 element sum over unsigned int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    unsigned int source[7];
    unsigned int target[7];
    for (int i = 0; i < 7; i++) {
        source[i] = (unsigned int)(i + 1);
    }

    unsigned int total = 0;
    for (int i = 0; i < 7; i++) {
        total += source[i];
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
