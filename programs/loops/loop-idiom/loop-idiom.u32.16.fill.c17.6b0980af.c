// a 16 element fill over unsigned int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    unsigned int source[16];
    unsigned int target[16];
    for (int i = 0; i < 16; i++) {
        source[i] = (unsigned int)(i + 1);
    }

    for (int i = 0; i < 16; i++) {
        target[i] = (unsigned int)0u;
    }

    printf("%llu\n", (unsigned long long)(target[0]));
    printf("%llu\n", (unsigned long long)(target[15]));
    return 0;
}
