// a 16 element sum over int, which is a library call in disguise
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int source[16];
    int target[16];
    for (int i = 0; i < 16; i++) {
        source[i] = (int)(i + 1);
    }

    int total = 0;
    for (int i = 0; i < 16; i++) {
        total += source[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
