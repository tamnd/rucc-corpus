// three induction variables over 8 iterations on int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int scaled = 0, walked = 0;
    int counted = 0;
    int buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = (int)i;
        scaled += (int)(i * 4);
        walked += buffer[i];
        counted++;
    }

    printf("%lld\n", (long long)(scaled));
    printf("%lld\n", (long long)(walked));
    printf("%lld\n", (long long)(counted));
    return 0;
}
