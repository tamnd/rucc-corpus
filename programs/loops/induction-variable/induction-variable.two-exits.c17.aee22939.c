// a loop that two-exits, counted by the generator
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int stop_in = 5;

int main(void) {
    int stop = stop_in;

    int counted = 0;
    int reason = 0;
    for (int i = 0; i < 20; i++) {
        if (i == stop) { reason = 1; break; }
        counted++;
    }

    printf("%lld\n", (long long)(counted));
    printf("%lld\n", (long long)(reason));
    return 0;
}
