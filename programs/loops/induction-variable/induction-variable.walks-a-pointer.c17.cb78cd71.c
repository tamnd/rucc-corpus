// a loop that walks-a-pointer, counted by the generator
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int values[12];
    for (int i = 0; i < 12; i++) {
        values[i] = i * 2;
    }
    int counted = 0;
    int total = 0;
    for (int *at = values; at != values + 12; at += 3) {
        counted++;
        total += *at;
    }

    printf("%lld\n", (long long)(counted));
    printf("%lld\n", (long long)(total));
    return 0;
}
