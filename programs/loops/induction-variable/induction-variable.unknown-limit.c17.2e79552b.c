// a loop that unknown-limit, counted by the generator
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int limit_in = 7;

int main(void) {
    int limit = limit_in;

    int counted = 0;
    int total = 0;
    for (int i = 0; i < limit; i++) {
        counted++;
        total += i;
    }

    printf("%lld\n", (long long)(counted));
    printf("%lld\n", (long long)(total));
    return 0;
}
