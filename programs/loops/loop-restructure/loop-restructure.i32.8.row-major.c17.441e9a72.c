// a 8 by 8 int traversal, row-major
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    static int grid[8][8];
    int total = 0;

    for (int r = 0; r < 8; r++) {
        for (int c = 0; c < 8; c++) {
            grid[r][c] = (int)(r * 8 + c);
            total += grid[r][c];
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
