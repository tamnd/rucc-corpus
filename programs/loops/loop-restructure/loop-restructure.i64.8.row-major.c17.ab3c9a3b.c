// a 8 by 8 long long traversal, row-major
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    static long long grid[8][8];
    long long total = 0;

    for (int r = 0; r < 8; r++) {
        for (int c = 0; c < 8; c++) {
            grid[r][c] = (long long)(r * 8 + c);
            total += grid[r][c];
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
