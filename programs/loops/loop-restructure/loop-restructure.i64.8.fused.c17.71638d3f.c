// a 8 by 8 long long traversal, fused
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    static long long grid[8][8];
    long long total = 0;

    for (int at = 0; at < 64; at++) {
        int r = at / 8;
        int c = at % 8;
        grid[r][c] = (long long)(r * 8 + c);
        total += grid[r][c];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
