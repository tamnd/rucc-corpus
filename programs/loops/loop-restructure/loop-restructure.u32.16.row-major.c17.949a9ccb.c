// a 16 by 16 unsigned int traversal, row-major
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    static unsigned int grid[16][16];
    unsigned int total = 0;

    for (int r = 0; r < 16; r++) {
        for (int c = 0; c < 16; c++) {
            grid[r][c] = (unsigned int)(r * 16 + c);
            total += grid[r][c];
        }
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
