// a long long loop whose test writes to memory
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int counter;

static int step(void) {
    counter++;
    return counter < 8;
}

int main(void) {
    long long total = 0;
    while (step()) {
        total += (long long)counter;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
