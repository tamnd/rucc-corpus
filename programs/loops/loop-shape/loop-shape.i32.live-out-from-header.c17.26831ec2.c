// a int loop whose header defines a value read after the loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 20;

int main(void) {
    int bound = bound_in;

    int total = 0;
    int i = 0;
    int last = 0;
    while ((last = i * 3) < bound) {
        total += (int)last;
        i++;
    }

    printf("%lld\n", (long long)(last));

    printf("%lld\n", (long long)(total));
    return 0;
}
