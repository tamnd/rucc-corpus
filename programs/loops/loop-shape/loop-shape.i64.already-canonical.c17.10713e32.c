// a long long loop that is already in all five shapes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 8;

int main(void) {
    int bound = bound_in;

    long long total = 0;
    int i = 0;
    if (0 < bound) {
        do {
            total += (long long)i;
            i++;
        } while (i < bound);
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
