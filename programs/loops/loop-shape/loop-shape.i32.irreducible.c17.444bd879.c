// a int cycle of two blocks entered at both of them
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int start_odd_in = 0;

int main(void) {
    int start_odd = start_odd_in;

    int total = 0;
    int i = 0;
    if (start_odd) goto odd;
    even:
    if (i >= 8) goto done;
    total += (int)i;
    i++;
    goto odd;
    odd:
    if (i >= 8) goto done;
    total += (int)(2 * i);
    i++;
    goto even;
    done:;

    printf("%lld\n", (long long)(total));
    return 0;
}
