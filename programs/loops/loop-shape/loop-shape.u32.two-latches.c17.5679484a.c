// a unsigned int loop whose `continue` gives it a second back edge
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    unsigned int total = 0;
    for (int i = 0; i < 12; i++) {
        switch (i % 3) {
        case 0:
            total += 1;
            continue;
        case 1:
            total += 2;
            break;
        default:
            total += 3;
            break;
        }
        total += 10;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
