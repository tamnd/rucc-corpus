// a unsigned int loop header over the -Os limit of five
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 10;

int main(void) {
    int bound = bound_in;

    unsigned int total = 0;
    int i = 0;
    while ((i ^ 1) + (i ^ 2) + (i ^ 3) < bound) {
        total += (unsigned int)i;
        i++;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
