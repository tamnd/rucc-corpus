// a unsigned long long loop header over the -O1 limit of twenty
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 70;

int main(void) {
    int bound = bound_in;

    unsigned long long total = 0;
    int i = 0;
    while ((i ^ 1) + (i ^ 2) + (i ^ 3) + (i ^ 4) + (i ^ 5) + (i ^ 6) + (i ^ 7) + (i ^ 8) + (i ^ 9) + (i ^ 10) + (i ^ 11) < bound) {
        total += (unsigned long long)i;
        i++;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
