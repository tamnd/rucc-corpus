// a unsigned long long loop whose counter is read twice afterwards
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 10;

int main(void) {
    int bound = bound_in;

    unsigned long long total = 0;
    int i;
    for (i = 0; i < bound; i++) {
        total += (unsigned long long)i;
    }

    printf("%lld\n", (long long)(i));
    printf("%lld\n", (long long)(i * 2));

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
