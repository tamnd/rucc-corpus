// a int loop the ranges can prove runs at least once
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 13u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned bound = seed % 8u + 1u;
    int total = 0;
    unsigned i = 0;
    while (i < bound) {
        total += (int)i;
        i++;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
