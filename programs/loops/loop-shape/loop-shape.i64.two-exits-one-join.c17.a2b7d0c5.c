// a long long loop with two exits that leave to one block
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int first_in = 5;
static volatile int second_in = 9;

int main(void) {
    int first = first_in;
    int second = second_in;

    long long total = 0;
    for (int i = 0; i < 16; i++) {
        if (i == first) break;
        if (i == second) break;
        total += (long long)i;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
