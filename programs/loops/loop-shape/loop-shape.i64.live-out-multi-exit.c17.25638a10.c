// a long long loop read afterwards that leaves two ways
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 10;
static volatile int stop_in = 6;

int main(void) {
    int bound = bound_in;
    int stop = stop_in;

    long long total = 0;
    int i;
    for (i = 0; i < bound; i++) {
        if (i == stop) break;
        total += (long long)i;
    }

    printf("%lld\n", (long long)(i));

    printf("%lld\n", (long long)(total));
    return 0;
}
