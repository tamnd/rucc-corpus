// a unsigned long long loop the ranges can prove never runs
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 13u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned bound = seed % 4u;
    unsigned long long total = 0;
    unsigned i = 8;
    while (i < bound) {
        total += (unsigned long long)i;
        i++;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
