// a long long loop whose entry test the ranges cannot settle
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int bound_in = 9u;

int main(void) {
    unsigned int bound = bound_in;

    long long total = 0;
    unsigned i = 0;
    while (i < bound) {
        total += (long long)i;
        i++;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
