// a long long loop header over the -Os limit of five
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 10;

int main(void) {
    int bound = bound_in;

    long long total = 0;
    int i = 0;
    while ((i ^ 1) + (i ^ 2) + (i ^ 3) < bound) {
        total += (long long)i;
        i++;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
