// a unsigned int loop with two exits that leave to one block
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int first_in = 5;
static volatile int second_in = 9;

int main(void) {
    int first = first_in;
    int second = second_in;

    unsigned int total = 0;
    for (int i = 0; i < 16; i++) {
        if (i == first) break;
        if (i == second) break;
        total += (unsigned int)i;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
