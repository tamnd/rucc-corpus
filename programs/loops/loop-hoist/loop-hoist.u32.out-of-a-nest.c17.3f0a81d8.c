// an invariant in the inner loop of a unsigned int nest
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int a_in = 3u;
static volatile unsigned int b_in = 5u;

int main(void) {
    unsigned int a = a_in;
    unsigned int b = b_in;

    unsigned int total = 0;
    for (int o = 0; o < 4; o++) {
        for (int i = 0; i < 4; i++) {
            total += a * b + (unsigned int)(o + i);
        }
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
