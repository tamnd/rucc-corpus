// an invariant in the inner loop of a int nest
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 3;
static volatile int b_in = 5;

int main(void) {
    int a = a_in;
    int b = b_in;

    int total = 0;
    for (int o = 0; o < 4; o++) {
        for (int i = 0; i < 4; i++) {
            total += a * b + (int)(o + i);
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
