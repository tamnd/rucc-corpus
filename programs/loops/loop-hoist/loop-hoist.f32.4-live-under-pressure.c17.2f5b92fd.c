// 4 live float accumulators and a cheap invariant, which is what the other bank sees
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile float scale_in = 2;
static volatile float bias_in = 3;

int main(void) {

    float scale = scale_in;
    float bias = bias_in;
    float v0 = 0;
    float v1 = 0;
    float v2 = 0;
    float v3 = 0;
    for (int i = 0; i < 8; i++) {
        float k = scale + bias;
        v0 += k + (i + 0);
        v1 += k + (i + 1);
        v2 += k + (i + 2);
        v3 += k + (i + 3);
    }

    float total = v0 + v1 + v2 + v3;

    printf("%lld\n", (long long)(total));
    return 0;
}
