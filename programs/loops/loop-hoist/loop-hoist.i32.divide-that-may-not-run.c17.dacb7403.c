// an invariant divide by zero in a int loop that never runs
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int top_in = 84;
static volatile int bottom_in = 0;
static volatile int bound_in = 0;

int main(void) {
    int top = top_in;
    int bottom = bottom_in;
    int bound = bound_in;

    int total = 0;
    for (int i = 0; i < bound; i++) {
        total += top / bottom;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
