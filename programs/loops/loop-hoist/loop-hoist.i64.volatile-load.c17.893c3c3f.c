// a volatile read a long long loop must do every iteration
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int watched = 5;

int main(void) {

    long long total = 0;
    for (int i = 0; i < 8; i++) {
        total += (long long)(watched + i);
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
