// an invariant divide by zero in a long long loop that never runs
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long top_in = 84ll;
static volatile long long bottom_in = 0ll;
static volatile int bound_in = 0;

int main(void) {
    long long top = top_in;
    long long bottom = bottom_in;
    int bound = bound_in;

    long long total = 0;
    for (int i = 0; i < bound; i++) {
        total += top / bottom;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
