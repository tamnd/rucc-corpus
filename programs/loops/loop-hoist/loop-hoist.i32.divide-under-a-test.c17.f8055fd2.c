// an invariant divide by zero under a false test, int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int top_in = 84;
static volatile int bottom_in = 0;

int main(void) {
    int top = top_in;
    int bottom = bottom_in;

    int total = 0;
    for (int i = 0; i < 8; i++) {
        if (bottom) {
            total += top / bottom;
        } else {
            total += (int)i;
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
