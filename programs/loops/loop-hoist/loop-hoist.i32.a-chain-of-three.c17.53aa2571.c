// three invariant computations feeding each other, int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 3;
static volatile int b_in = 5;
static volatile int c_in = 2;

int main(void) {
    int a = a_in;
    int b = b_in;
    int c = c_in;

    int total = 0;
    for (int i = 0; i < 8; i++) {
        total += (a * b + c) * (a + b) + (int)i;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
