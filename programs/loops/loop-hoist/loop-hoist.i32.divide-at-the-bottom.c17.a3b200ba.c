// an invariant divide in a int loop that always runs
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int top_in = 84;
static volatile int bottom_in = 4;
static volatile int bound_in = 8;

int main(void) {
    int top = top_in;
    int bottom = bottom_in;
    int bound = bound_in;

    int total = 0;
    int i = 0;
    do {
        total += top / bottom + (int)i;
        i++;
    } while (i < bound);

    printf("%lld\n", (long long)(total));
    return 0;
}
