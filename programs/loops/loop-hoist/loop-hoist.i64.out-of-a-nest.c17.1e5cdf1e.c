// an invariant in the inner loop of a long long nest
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 3ll;
static volatile long long b_in = 5ll;

int main(void) {
    long long a = a_in;
    long long b = b_in;

    long long total = 0;
    for (int o = 0; o < 4; o++) {
        for (int i = 0; i < 4; i++) {
            total += a * b + (long long)(o + i);
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
