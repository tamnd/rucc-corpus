// an invariant divide by zero under a false test, unsigned int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int top_in = 84u;
static volatile unsigned int bottom_in = 0u;

int main(void) {
    unsigned int top = top_in;
    unsigned int bottom = bottom_in;

    unsigned int total = 0;
    for (int i = 0; i < 8; i++) {
        if (bottom) {
            total += top / bottom;
        } else {
            total += (unsigned int)i;
        }
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
