// an invariant value a unsigned long long loop stores every iteration
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int written;
static volatile unsigned long long a_in = 3ull;
static volatile unsigned long long b_in = 5ull;

int main(void) {
    unsigned long long a = a_in;
    unsigned long long b = b_in;

    unsigned long long total = 0;
    for (int i = 0; i < 8; i++) {
        written = (int)(a + b);
        total += (unsigned long long)i;
    }

    printf("%lld\n", (long long)(written));

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
