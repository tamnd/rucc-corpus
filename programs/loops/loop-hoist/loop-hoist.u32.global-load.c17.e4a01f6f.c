// a unsigned int loop reading a global nothing in it writes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int seed;
static volatile int start_in = 3;

int main(void) {
    int start = start_in;

    seed = start;
    unsigned int total = 0;
    for (int i = 0; i < 8; i++) {
        total += (unsigned int)(seed + i);
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
