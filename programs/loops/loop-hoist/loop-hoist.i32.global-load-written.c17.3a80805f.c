// a int loop reading a global the body writes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int seed;

static void bump(void) {
    seed++;
}
static volatile int start_in = 3;

int main(void) {
    int start = start_in;

    seed = start;
    int total = 0;
    for (int i = 0; i < 8; i++) {
        total += (int)seed;
        bump();
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
