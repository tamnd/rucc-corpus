// 6 live double accumulators and a cheap invariant, which is what the other bank sees
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile double scale_in = 2;
static volatile double bias_in = 3;

int main(void) {

    double scale = scale_in;
    double bias = bias_in;
    double v0 = 0;
    double v1 = 0;
    double v2 = 0;
    double v3 = 0;
    double v4 = 0;
    double v5 = 0;
    for (int i = 0; i < 8; i++) {
        double k = scale + bias;
        v0 += k + (i + 0);
        v1 += k + (i + 1);
        v2 += k + (i + 2);
        v3 += k + (i + 3);
        v4 += k + (i + 4);
        v5 += k + (i + 5);
    }

    double total = v0 + v1 + v2 + v3 + v4 + v5;

    printf("%lld\n", (long long)(total));
    return 0;
}
