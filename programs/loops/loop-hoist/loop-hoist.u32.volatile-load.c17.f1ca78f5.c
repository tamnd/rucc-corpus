// a volatile read a unsigned int loop must do every iteration
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int watched = 5;

int main(void) {

    unsigned int total = 0;
    for (int i = 0; i < 8; i++) {
        total += (unsigned int)(watched + i);
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
