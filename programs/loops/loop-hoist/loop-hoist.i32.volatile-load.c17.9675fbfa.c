// a volatile read a int loop must do every iteration
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int watched = 5;

int main(void) {

    int total = 0;
    for (int i = 0; i < 8; i++) {
        total += (int)(watched + i);
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
