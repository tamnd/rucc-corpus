// an invariant divide by zero under a false test, long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long top_in = 84ll;
static volatile long long bottom_in = 0ll;

int main(void) {
    long long top = top_in;
    long long bottom = bottom_in;

    long long total = 0;
    for (int i = 0; i < 8; i++) {
        if (bottom) {
            total += top / bottom;
        } else {
            total += (long long)i;
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
