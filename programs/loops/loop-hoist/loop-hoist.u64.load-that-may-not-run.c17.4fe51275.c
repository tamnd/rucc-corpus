// a unsigned long long loop that never runs, over a pointer that is null
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 0;

int main(void) {
    int bound = bound_in;

    static int cell = 41;
    int *at = bound ? &cell : (int *)0;
    unsigned long long total = 0;
    for (int i = 0; i < bound; i++) {
        total += (unsigned long long)*at;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
