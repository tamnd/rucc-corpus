// a unsigned int loop that always runs, over a valid pointer
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int bound_in = 8;

int main(void) {
    int bound = bound_in;

    static int cell = 41;
    int *at = bound ? &cell : (int *)0;
    unsigned int total = 0;
    int i = 0;
    do {
        total += (unsigned int)(*at + i);
        i++;
    } while (i < bound);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
