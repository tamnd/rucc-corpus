// a nest whose inner bound is not known, int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int inner_in = 4;

int main(void) {
    int inner = inner_in;

    int total = 0;
    for (int x = 0; x < 3; x++) {
        for (int y = 0; y < inner; y++) {
            total += (int)y;
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
