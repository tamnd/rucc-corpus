// a body that calls something which counts the calls, unsigned long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int calls;

static int bump(int x) {
    calls++;
    return x + 1;
}

int main(void) {

    int stepped = 0;
    for (int i = 0; i < 5; i++) {
        stepped = bump(stepped);
    }

    printf("%lld\n", (long long)(calls));
    unsigned long long total = (unsigned long long)stepped;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
