// a body that calls something which counts the calls, int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int calls;

static int bump(int x) {
    calls++;
    return x + 1;
}

int main(void) {

    int stepped = 0;
    for (int i = 0; i < 5; i++) {
        stepped = bump(stepped);
    }

    printf("%lld\n", (long long)(calls));
    int total = (int)stepped;

    printf("%lld\n", (long long)(total));
    return 0;
}
