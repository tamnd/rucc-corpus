// a nest whose loops both have a known count, unsigned long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    unsigned long long total = 0;
    for (int x = 0; x < 4; x++) {
        for (int y = 0; y < 3; y++) {
            total += (unsigned long long)(x * y);
        }
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
