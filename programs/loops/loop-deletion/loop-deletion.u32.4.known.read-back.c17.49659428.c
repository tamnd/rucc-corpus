// a 4 iteration unsigned int loop with a known bound whose total is read afterwards
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    unsigned int total = 0;
    for (int i = 0; i < 4; i++) {
        total += (unsigned int)3u;
    }

    unsigned int aside = 0;
    for (int i = 0; i < 8; i++) {
        aside += (unsigned int)i;
    }

    printf("%llu\n", (unsigned long long)(aside));
    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
