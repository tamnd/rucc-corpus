// a dead 16 iteration int loop with an known bound
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    int unread = 0;
    for (int i = 0; i < 16; i++) {
        unread += (int)(i * 7 + 1);
    }

    int total = 0;
    for (int i = 0; i < 16; i++) {
        total += (int)i;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
