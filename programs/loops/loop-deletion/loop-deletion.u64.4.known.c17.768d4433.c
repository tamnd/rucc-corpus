// a dead 4 iteration unsigned long long loop with an known bound
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    unsigned long long unread = 0;
    for (int i = 0; i < 4; i++) {
        unread += (unsigned long long)(i * 7 + 1);
    }

    unsigned long long total = 0;
    for (int i = 0; i < 4; i++) {
        total += (unsigned long long)i;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
