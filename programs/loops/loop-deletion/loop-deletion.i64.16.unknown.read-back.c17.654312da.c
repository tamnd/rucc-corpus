// a 16 iteration long long loop with an unknown bound whose total is read afterwards
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int limit_in = 16;

int main(void) {
    int limit = limit_in;

    long long total = 0;
    for (int i = 0; i < limit; i++) {
        total += (long long)3ll;
    }

    long long aside = 0;
    for (int i = 0; i < 8; i++) {
        aside += (long long)i;
    }

    printf("%lld\n", (long long)(aside));
    printf("%lld\n", (long long)(total));
    return 0;
}
