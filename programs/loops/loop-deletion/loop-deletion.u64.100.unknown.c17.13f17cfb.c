// a dead 100 iteration unsigned long long loop with an unknown bound
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int limit_in = 100;

int main(void) {
    int limit = limit_in;

    unsigned long long unread = 0;
    for (int i = 0; i < limit; i++) {
        unread += (unsigned long long)(i * 7 + 1);
    }

    unsigned long long total = 0;
    for (int i = 0; i < limit; i++) {
        total += (unsigned long long)i;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
