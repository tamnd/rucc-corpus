// a dead 100 iteration unsigned int loop with an known bound
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    unsigned int unread = 0;
    for (int i = 0; i < 100; i++) {
        unread += (unsigned int)(i * 7 + 1);
    }

    unsigned int total = 0;
    for (int i = 0; i < 100; i++) {
        total += (unsigned int)i;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
