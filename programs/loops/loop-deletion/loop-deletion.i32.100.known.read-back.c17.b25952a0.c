// a 100 iteration int loop with a known bound whose total is read afterwards
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    int total = 0;
    for (int i = 0; i < 100; i++) {
        total += (int)3;
    }

    int aside = 0;
    for (int i = 0; i < 8; i++) {
        aside += (int)i;
    }

    printf("%lld\n", (long long)(aside));
    printf("%lld\n", (long long)(total));
    return 0;
}
