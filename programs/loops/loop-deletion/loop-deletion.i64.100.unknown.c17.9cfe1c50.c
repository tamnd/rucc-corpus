// a dead 100 iteration long long loop with an unknown bound
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int limit_in = 100;

int main(void) {
    int limit = limit_in;

    long long unread = 0;
    for (int i = 0; i < limit; i++) {
        unread += (long long)(i * 7 + 1);
    }

    long long total = 0;
    for (int i = 0; i < limit; i++) {
        total += (long long)i;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
