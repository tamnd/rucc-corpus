// a 100 iteration long long loop with a known bound whose total is read afterwards
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    long long total = 0;
    for (int i = 0; i < 100; i++) {
        total += (long long)3ll;
    }

    long long aside = 0;
    for (int i = 0; i < 8; i++) {
        aside += (long long)i;
    }

    printf("%lld\n", (long long)(aside));
    printf("%lld\n", (long long)(total));
    return 0;
}
