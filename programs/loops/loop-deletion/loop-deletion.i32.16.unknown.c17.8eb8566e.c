// a dead 16 iteration int loop with an unknown bound
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int limit_in = 16;

int main(void) {
    int limit = limit_in;

    int unread = 0;
    for (int i = 0; i < limit; i++) {
        unread += (int)(i * 7 + 1);
    }

    int total = 0;
    for (int i = 0; i < limit; i++) {
        total += (int)i;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
