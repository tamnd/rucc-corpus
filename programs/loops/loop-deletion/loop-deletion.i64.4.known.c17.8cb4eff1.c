// a dead 4 iteration long long loop with an known bound
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {

    long long unread = 0;
    for (int i = 0; i < 4; i++) {
        unread += (long long)(i * 7 + 1);
    }

    long long total = 0;
    for (int i = 0; i < 4; i++) {
        total += (long long)i;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
