// an invariant test inside a 4 iteration long long loop, flag 1
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int flag_in = 1;

int main(void) {
    int flag = flag_in;

    long long total = 0;
    for (int i = 0; i < 4; i++) {
        if (flag) {
            total += (long long)(i * 2);
        } else {
            total += (long long)(i + 1);
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
