// an invariant test inside a 4 iteration int loop, flag 1
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int flag_in = 1;

int main(void) {
    int flag = flag_in;

    int total = 0;
    for (int i = 0; i < 4; i++) {
        if (flag) {
            total += (int)(i * 2);
        } else {
            total += (int)(i + 1);
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
