// an invariant test inside a 2 iteration int loop, flag 0
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int flag_in = 0;

int main(void) {
    int flag = flag_in;

    int total = 0;
    for (int i = 0; i < 2; i++) {
        if (flag) {
            total += (int)(i * 2);
        } else {
            total += (int)(i + 1);
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
