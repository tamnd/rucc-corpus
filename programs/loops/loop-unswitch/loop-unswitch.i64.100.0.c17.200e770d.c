// an invariant test inside a 100 iteration long long loop, flag 0
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int flag_in = 0;

int main(void) {
    int flag = flag_in;

    long long total = 0;
    for (int i = 0; i < 100; i++) {
        if (flag) {
            total += (long long)(i * 2);
        } else {
            total += (long long)(i + 1);
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
