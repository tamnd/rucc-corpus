// setjmp and longjmp, stores are not sunk
// Generated by rucc-corpus. Edit the generator, not this file.

#include <setjmp.h>

int printf(const char *, ...);

static jmp_buf home;
static volatile int port;
static void raise_it(void) { longjmp(home, 3); }

int main(void) {

    volatile int answer = 0;
    if (setjmp(home) == 0) {
        port = 11;
        raise_it();
        port = 22;
    } else {
        answer = port;
    }

    printf("%lld\n", (long long)(port));
    printf("%lld\n", (long long)(answer));
    return 0;
}
