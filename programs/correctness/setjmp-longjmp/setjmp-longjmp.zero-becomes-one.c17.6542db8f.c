// setjmp and longjmp, zero becomes one
// Generated by rucc-corpus. Edit the generator, not this file.

#include <setjmp.h>

int printf(const char *, ...);

static jmp_buf home;

int main(void) {

    volatile int answer = -1;
    switch (setjmp(home)) {
    case 0:
        longjmp(home, 0);
        break;
    case 1:
        answer = 1;
        break;
    default:
        answer = 99;
        break;
    }

    printf("%lld\n", (long long)(answer));
    return 0;
}
