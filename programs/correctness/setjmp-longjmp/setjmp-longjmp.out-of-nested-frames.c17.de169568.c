// setjmp and longjmp, out of nested frames
// Generated by rucc-corpus. Edit the generator, not this file.

#include <setjmp.h>

int printf(const char *, ...);

static jmp_buf home;
static volatile int trail;
static void third(void) {
    trail = trail * 10 + 3;
    longjmp(home, 5);
}
static void second(void) {
    trail = trail * 10 + 2;
    third();
    trail = trail * 10 + 9;
}
static void first(void) {
    trail = trail * 10 + 1;
    second();
    trail = trail * 10 + 8;
}

int main(void) {

    volatile int answer = 0;
    if (setjmp(home) == 0) {
        first();
        trail = trail * 10 + 7;
    } else {
        answer = 5;
    }

    printf("%lld\n", (long long)(trail));
    printf("%lld\n", (long long)(answer));
    return 0;
}
