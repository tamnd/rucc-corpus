// setjmp and longjmp, the value comes back
// Generated by rucc-corpus. Edit the generator, not this file.

#include <setjmp.h>

int printf(const char *, ...);

static jmp_buf home;
static volatile int arrivals;

int main(void) {

    volatile int answer = 0;
    switch (setjmp(home)) {
    case 0:
        arrivals = arrivals + 1;
        longjmp(home, 7);
        break;
    case 7:
        answer = 70;
        break;
    default:
        answer = -1;
        break;
    }

    printf("%lld\n", (long long)(arrivals));
    printf("%lld\n", (long long)(answer));
    return 0;
}
