// setjmp and longjmp, past an inner handler
// Generated by rucc-corpus. Edit the generator, not this file.

#include <setjmp.h>

int printf(const char *, ...);

static jmp_buf outer;
static jmp_buf inner;
static volatile int trail;

int main(void) {

    volatile int answer = 0;
    if (setjmp(outer) == 0) {
        if (setjmp(inner) == 0) {
            trail = trail * 10 + 1;
            longjmp(outer, 2);
        }
        trail = trail * 10 + 9;
    } else {
        answer = 2;
    }

    printf("%lld\n", (long long)(trail));
    printf("%lld\n", (long long)(answer));
    return 0;
}
