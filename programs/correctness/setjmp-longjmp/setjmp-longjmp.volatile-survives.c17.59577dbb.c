// setjmp and longjmp, volatile survives
// Generated by rucc-corpus. Edit the generator, not this file.

#include <setjmp.h>

int printf(const char *, ...);

static jmp_buf home;

int main(void) {

    volatile int counter = 0;
    counter = counter + 10;
    if (setjmp(home) == 0) {
        counter = counter + 5;
        longjmp(home, 1);
    }

    printf("%lld\n", (long long)(counter));
    return 0;
}
