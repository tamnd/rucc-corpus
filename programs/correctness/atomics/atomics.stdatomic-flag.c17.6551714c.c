// atomic_flag through the header, initialised the way the header says
// Generated by rucc-corpus. Edit the generator, not this file.

#include <stdatomic.h>

int printf(const char *, ...);

static atomic_flag gate = ATOMIC_FLAG_INIT;

int main(void) {

    printf("%lld\n", (long long)(atomic_flag_test_and_set(&gate)));
    printf("%lld\n", (long long)(atomic_flag_test_and_set(&gate)));
    atomic_flag_clear(&gate);
    printf("%lld\n", (long long)(atomic_flag_test_and_set_explicit(&gate, memory_order_acquire)));
    atomic_flag_clear_explicit(&gate, memory_order_release);
    printf("%lld\n", (long long)(atomic_flag_test_and_set(&gate)));
    return 0;
}
