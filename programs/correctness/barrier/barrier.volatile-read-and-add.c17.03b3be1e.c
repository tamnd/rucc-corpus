// a volatile-read-and-add the compiler must not optimize away
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int step_in = 4;
static volatile int gauge = 38;
static int read_and_add(volatile int *at, int by) { return *at + by; }

int main(void) {
    int step = step_in;

    int once = read_and_add(&gauge, step);
    int twice = read_and_add(&gauge, once);

    printf("%lld\n", (long long)(once));
    printf("%lld\n", (long long)(twice));
    return 0;
}
