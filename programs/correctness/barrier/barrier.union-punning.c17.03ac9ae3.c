// a union-punning the compiler must not optimize away
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

union both { unsigned int whole; unsigned char parts[4]; };

int main(void) {

    union both held;
    held.whole = 0;
    held.parts[0] = 0xff;

    printf("%lld\n", (long long)(held.parts[0]));
    printf("%lld\n", (long long)(held.whole != 0));
    return 0;
}
