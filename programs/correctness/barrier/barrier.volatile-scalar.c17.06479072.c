// a volatile-scalar the compiler must not optimize away
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int gate = 5;

int main(void) {

    int first = gate;
    int second = gate;
    gate = first + second;

    printf("%lld\n", (long long)(gate));
    printf("%lld\n", (long long)(first + second));
    return 0;
}
