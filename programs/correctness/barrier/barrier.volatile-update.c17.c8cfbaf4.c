// a volatile-update the compiler must not optimize away
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int by_in = 3;
static volatile int port = 10;
static void bump(volatile int *at, int by) { *at += by; }

int main(void) {
    int by = by_in;

    bump(&port, by);
    bump(&port, by);
    bump(&port, 1);

    printf("%lld\n", (long long)(port));
    return 0;
}
