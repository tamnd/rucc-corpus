// a volatile-field the compiler must not optimize away
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

struct device { int plain; volatile int live; };
static struct device dev = { 2, 40 };

int main(void) {

    dev.plain = dev.plain + 1;
    dev.live = dev.live + 1;
    int total = dev.plain + dev.live;

    printf("%lld\n", (long long)(total));
    return 0;
}
