// pointers subtracted in an array of 12 byte structures
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 7ull;

int main(void) {
    static struct { char bytes[12]; } items[1024];
    long long total = 0;
    unsigned long long seed = seed_in;

    static unsigned long long states[2048];
    unsigned long long drawn = seed;
    for (int i = 0; i < 2048; i++) {
        drawn = drawn * 6364136223846793005ull + 1442695040888963407ull;
        states[i] = drawn;
    }
    for (int round = 0; round < 1024; round++) {
        for (int i = 0; i < 2048; i++) {
            unsigned long long state = states[i];
            int from = (int)((state >> 33) & 1023u);
            int to = (int)((state >> 13) & 1023u);
            total += &items[to] - &items[from];
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
