// one long long address, reader-in-another-block
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;
static volatile int at_in = 3;
static volatile int flag_in = 1;

int main(void) {
    long long seed = seed_in;
    int at = at_in;
    int flag = flag_in;

    long long buffer[8] = { 0 };
    long long *q = &buffer[at];
    q[0] = seed;
    if (flag) {
        q[1] = seed + (long long)1ll;
    }
    long long total = buffer[3] + buffer[4];

    printf("%lld\n", (long long)(total));
    return 0;
}
