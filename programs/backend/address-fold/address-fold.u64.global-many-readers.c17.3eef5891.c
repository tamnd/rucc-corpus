// one unsigned long long address, global-many-readers
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static unsigned long long shared[4];

int main(void) {
    unsigned long long seed = seed_in;

    shared[0] = seed;
    shared[1] = seed + (unsigned long long)1ull;
    shared[2] = seed + (unsigned long long)2ull;
    shared[3] = seed + (unsigned long long)3ull;
    unsigned long long total = shared[0] + shared[1] + shared[2] + shared[3];

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
