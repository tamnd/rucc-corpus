// one unsigned long long address, filled-then-read
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;

int main(void) {
    unsigned long long seed = seed_in;

    unsigned long long grid[8];
    for (int i = 0; i < 8; i++) {
        grid[i] = seed + (unsigned long long)i;
    }
    unsigned long long total = 0;
    for (int i = 0; i < 8; i++) {
        total += grid[i];
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
