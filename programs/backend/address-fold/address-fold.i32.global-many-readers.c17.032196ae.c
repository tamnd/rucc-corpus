// one int address, global-many-readers
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;
static int shared[4];

int main(void) {
    int seed = seed_in;

    shared[0] = seed;
    shared[1] = seed + (int)1;
    shared[2] = seed + (int)2;
    shared[3] = seed + (int)3;
    int total = shared[0] + shared[1] + shared[2] + shared[3];

    printf("%lld\n", (long long)(total));
    return 0;
}
