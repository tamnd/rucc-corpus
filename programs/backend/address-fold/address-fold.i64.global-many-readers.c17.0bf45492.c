// one long long address, global-many-readers
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;
static long long shared[4];

int main(void) {
    long long seed = seed_in;

    shared[0] = seed;
    shared[1] = seed + (long long)1ll;
    shared[2] = seed + (long long)2ll;
    shared[3] = seed + (long long)3ll;
    long long total = shared[0] + shared[1] + shared[2] + shared[3];

    printf("%lld\n", (long long)(total));
    return 0;
}
