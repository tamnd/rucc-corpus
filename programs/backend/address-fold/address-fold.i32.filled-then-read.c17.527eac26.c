// one int address, filled-then-read
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;

int main(void) {
    int seed = seed_in;

    int grid[8];
    for (int i = 0; i < 8; i++) {
        grid[i] = seed + (int)i;
    }
    int total = 0;
    for (int i = 0; i < 8; i++) {
        total += grid[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
