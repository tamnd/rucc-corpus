// one unsigned int address, global-many-readers
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 5u;
static unsigned int shared[4];

int main(void) {
    unsigned int seed = seed_in;

    shared[0] = seed;
    shared[1] = seed + (unsigned int)1u;
    shared[2] = seed + (unsigned int)2u;
    shared[3] = seed + (unsigned int)3u;
    unsigned int total = shared[0] + shared[1] + shared[2] + shared[3];

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
