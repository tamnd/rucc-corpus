// a int joined and then asked whether it is zero
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 40;
static volatile int b_in = 3;

int main(void) {
    int a = a_in;
    int b = b_in;

    int joined = (int)(a | b);
    int total = (joined != 0) ? 5 : 9;
    total = total + (int)(joined & 7);

    printf("%lld\n", (long long)(total));
    return 0;
}
