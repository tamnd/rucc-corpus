// a unsigned int compared once for a value and once for a branch
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int a_in = 40u;
static volatile unsigned int b_in = 15u;

int main(void) {
    unsigned int a = a_in;
    unsigned int b = b_in;

    int total = (a == b);
    if (a != b) {
        total = total + 7;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
