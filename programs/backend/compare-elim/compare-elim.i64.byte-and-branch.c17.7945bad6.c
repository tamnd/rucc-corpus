// a long long compared once for a value and once for a branch
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 40ll;
static volatile long long b_in = 15ll;

int main(void) {
    long long a = a_in;
    long long b = b_in;

    int total = (a == b);
    if (a != b) {
        total = total + 7;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
