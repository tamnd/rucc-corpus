// a int masked, written over, and then asked whether it is zero
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 40;
static volatile int mask_in = 24;
static volatile int spare_in = 0;

int main(void) {
    int a = a_in;
    int mask = mask_in;
    int spare = spare_in;

    int value = (int)(a & mask);
    int first = (value != 0);
    value = spare;
    int second = (value == 0);
    int total = first * 10 + second;

    printf("%lld\n", (long long)(total));
    return 0;
}
