// a int masked and then asked whether it is zero
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 40;
static volatile int mask_in = 24;

int main(void) {
    int a = a_in;
    int mask = mask_in;

    int masked = (int)(a & mask);
    int total = 0;
    if (masked) {
        total = total + 3;
    }
    total = total + (int)masked * 2;

    printf("%lld\n", (long long)(total));
    return 0;
}
