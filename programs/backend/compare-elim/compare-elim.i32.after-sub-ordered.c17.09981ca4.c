// a int subtracted and then asked which side of zero it is on
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 15;
static volatile int b_in = 40;

int main(void) {
    int a = a_in;
    int b = b_in;

    int d = (int)(a - b);
    int total = (d < 0) ? 4 : 8;
    total = total + (int)(d + 30);

    printf("%lld\n", (long long)(total));
    return 0;
}
