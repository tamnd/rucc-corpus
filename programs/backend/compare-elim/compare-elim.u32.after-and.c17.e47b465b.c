// a unsigned int masked and then asked whether it is zero
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int a_in = 40u;
static volatile unsigned int mask_in = 24u;

int main(void) {
    unsigned int a = a_in;
    unsigned int mask = mask_in;

    unsigned int masked = (unsigned int)(a & mask);
    int total = 0;
    if (masked) {
        total = total + 3;
    }
    total = total + (int)masked * 2;

    printf("%lld\n", (long long)(total));
    return 0;
}
