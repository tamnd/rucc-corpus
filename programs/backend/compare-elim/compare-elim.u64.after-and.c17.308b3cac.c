// a unsigned long long masked and then asked whether it is zero
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long a_in = 40ull;
static volatile unsigned long long mask_in = 24ull;

int main(void) {
    unsigned long long a = a_in;
    unsigned long long mask = mask_in;

    unsigned long long masked = (unsigned long long)(a & mask);
    int total = 0;
    if (masked) {
        total = total + 3;
    }
    total = total + (int)masked * 2;

    printf("%lld\n", (long long)(total));
    return 0;
}
