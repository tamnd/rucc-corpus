// a long long subtracted and then asked whether the answer is zero
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 40ll;
static volatile long long b_in = 40ll;

int main(void) {
    long long a = a_in;
    long long b = b_in;

    long long d = (long long)(a - b);
    int total = (d == 0) ? 6 : 9;
    total = total + (int)d;

    printf("%lld\n", (long long)(total));
    return 0;
}
