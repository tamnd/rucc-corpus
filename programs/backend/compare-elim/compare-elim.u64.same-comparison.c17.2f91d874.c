// a unsigned long long compared with the same pair twice
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long a_in = 40ull;
static volatile unsigned long long b_in = 15ull;

int main(void) {
    unsigned long long a = a_in;
    unsigned long long b = b_in;

    int same = (a == b);
    int differ = (a != b);
    int total = same * 4 + differ;

    printf("%lld\n", (long long)(total));
    return 0;
}
