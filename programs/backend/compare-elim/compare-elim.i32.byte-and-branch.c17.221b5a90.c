// a int compared once for a value and once for a branch
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 40;
static volatile int b_in = 15;

int main(void) {
    int a = a_in;
    int b = b_in;

    int total = (a == b);
    if (a != b) {
        total = total + 7;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
