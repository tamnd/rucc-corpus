// one long long load compared, index-written-between
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;
static volatile int at_in = 3;
static volatile int other_in = 6;

int main(void) {
    long long seed = seed_in;
    int at = at_in;
    int other = other_in;

    long long buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (long long)i;
    }
    long long value = buffer[at];
    at = other;
    int total = (value < 10) ? 4 : 7;
    total = total + (int)buffer[at];

    printf("%lld\n", (long long)(total));
    return 0;
}
