// one unsigned char load compared, branch
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned char seed_in = 5u;
static volatile int at_in = 3;

int main(void) {
    unsigned char seed = seed_in;
    int at = at_in;

    unsigned char buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned char)i;
    }
    int total = 0;
    if (buffer[at] > seed) {
        total = total + 9;
    }
    total = total + 3;

    printf("%lld\n", (long long)(total));
    return 0;
}
