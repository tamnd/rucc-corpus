// one unsigned char load compared, call-between
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned char seed_in = 5u;
static volatile int at_in = 3;
static volatile int noise_count;
static void noise(void) {
    noise_count = noise_count + 1;
}
static void (*volatile jump)(void) = noise;

int main(void) {
    unsigned char seed = seed_in;
    int at = at_in;

    unsigned char buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned char)i;
    }
    unsigned char value = buffer[at];
    jump();
    int total = (seed < value) ? 4 : 7;

    printf("%lld\n", (long long)(total));
    return 0;
}
