// one unsigned long long load compared, constant-equal
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static volatile int at_in = 3;

int main(void) {
    unsigned long long seed = seed_in;
    int at = at_in;

    unsigned long long buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned long long)i;
    }
    int total = (buffer[at] == 8) ? 4 : 7;

    printf("%lld\n", (long long)(total));
    return 0;
}
