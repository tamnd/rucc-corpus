// one int load compared, comparison-in-another-block
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;
static volatile int at_in = 3;
static volatile int flag_in = 1;

int main(void) {
    int seed = seed_in;
    int at = at_in;
    int flag = flag_in;

    int buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (int)i;
    }
    int value = buffer[at];
    int total = 3;
    if (flag) {
        total = (seed < value) ? 4 : 7;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
