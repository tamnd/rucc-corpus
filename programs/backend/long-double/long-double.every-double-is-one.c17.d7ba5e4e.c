// long double, every double is one
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile double values[4] = { 0.5, -2.25, 1048576.0, 1e9 };

int main(void) {

    int kept = 1;
    for (int i = 0; i < 4; i++) {
        double narrow = values[i];
        long double wide = narrow;
        if ((double)wide != narrow) {
            kept = 0;
        }
        if (wide != narrow) {
            kept = 0;
        }
    }

    printf("%lld\n", (long long)(kept));
    return 0;
}
