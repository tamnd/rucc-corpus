// long double, through varargs
// Generated by rucc-corpus. Edit the generator, not this file.

#include <stdarg.h>

int printf(const char *, ...);

static long double total(int count, ...) {
    va_list rest;
    va_start(rest, count);
    long double sum = 0.0L;
    for (int i = 0; i < count; i++) {
        sum = sum + va_arg(rest, long double);
    }
    va_end(rest);
    return sum;
}
static long double weighted(int count, ...) {
    va_list rest;
    va_start(rest, count);
    long double sum = 0.0L;
    for (int i = 0; i < count; i++) {
        int weight = va_arg(rest, int);
        sum = sum + va_arg(rest, long double) * weight;
    }
    va_end(rest);
    return sum;
}

int main(void) {

    printf("%lld\n", (long long)(total(5, 1.0L, 2.0L, 4.0L, 8.0L, 16.0L)));
    printf("%lld\n", (long long)(weighted(3, 2, 10.0L, 3, 100.0L, 4, 1000.0L)));
    return 0;
}
