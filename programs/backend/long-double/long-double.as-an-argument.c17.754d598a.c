// long double, as an argument
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long double blend(long double a, long double b, long double c,
                         long double d, long double e, long double f,
                         long double g, long double h) {
    return a * 1.0L + b * 2.0L + c * 3.0L + d * 4.0L
         + e * 5.0L + f * 6.0L + g * 7.0L + h * 8.0L;
}
static long double back(long double value) { return value * 2.0L; }
static volatile int seed_in = 1;

int main(void) {
    int seed = seed_in;

    long double one = seed;

    printf("%lld\n", (long long)(blend(one, 2.0L, 3.0L, 4.0L, 5.0L, 6.0L, 7.0L, 8.0L)));
    printf("%lld\n", (long long)(back(21.0L)));
    printf("%lld\n", (long long)(back(back(back(one)))));
    return 0;
}
