// long double, in a struct
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

struct wide { long double value; int tag; };
static struct wide scale(struct wide held, int by) {
    struct wide out;
    out.value = held.value * by;
    out.tag = held.tag + by;
    return out;
}
static long double sum(struct wide first, struct wide second) {
    return first.value + second.value;
}
static volatile int seven_in = 7;

int main(void) {
    int seven = seven_in;

    struct wide start;
    start.value = 12.0L;
    start.tag = 5;
    struct wide grown = scale(start, seven);

    printf("%lld\n", (long long)(grown.value));
    printf("%lld\n", (long long)(grown.tag));
    printf("%lld\n", (long long)(sum(start, grown)));
    return 0;
}
