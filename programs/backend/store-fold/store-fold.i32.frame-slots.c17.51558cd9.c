// one int read and write, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;
static int *volatile hold;

int main(void) {
    int seed = seed_in;

    int one = seed;
    int two = seed + (int)1;
    hold = &one;
    hold = &two;
    two = one + seed;
    int total = one + two;

    printf("%lld\n", (long long)(total));
    return 0;
}
