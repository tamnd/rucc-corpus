// one unsigned long long read and write, bitwise-and
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static volatile int at_in = 3;
static volatile int back_in = 3;
static volatile unsigned long long mask_in = 12ull;

int main(void) {
    unsigned long long seed = seed_in;
    int at = at_in;
    int back = back_in;
    unsigned long long mask = mask_in;

    unsigned long long buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned long long)i;
    }
    buffer[at] &= mask;
    unsigned long long total = buffer[back];

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
