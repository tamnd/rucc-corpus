// one unsigned long long read and write, two-readers
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static volatile int at_in = 3;
static volatile int back_in = 3;

int main(void) {
    unsigned long long seed = seed_in;
    int at = at_in;
    int back = back_in;

    unsigned long long buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned long long)i;
    }
    unsigned long long value = buffer[at];
    buffer[at] = value + seed;
    unsigned long long total = buffer[back] + value;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
