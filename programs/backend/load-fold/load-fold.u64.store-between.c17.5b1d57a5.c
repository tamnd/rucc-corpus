// one unsigned long long load, store-between
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long corpus_sink;

static volatile unsigned long long seed_in = 5ull;
static volatile int at_in = 3;
static volatile int other_in = 6;

int main(void) {
    unsigned long long seed = seed_in;
    int at = at_in;
    int other = other_in;

    unsigned long long buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned long long)i;
    }
    unsigned long long value = buffer[at];
    buffer[other] = seed;
    unsigned long long total = seed + value;
    corpus_sink = (long long)(buffer[0]);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
