// one int load, narrower-load
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;
static volatile int at_in = 3;

int main(void) {
    int seed = seed_in;
    int at = at_in;

    unsigned char bytes[8];
    for (int i = 0; i < 8; i++) {
        bytes[i] = (unsigned char)(seed + i);
    }
    int total = seed + bytes[at];

    printf("%lld\n", (long long)(total));
    return 0;
}
