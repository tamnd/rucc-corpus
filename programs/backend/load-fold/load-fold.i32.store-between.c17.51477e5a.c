// one int load, store-between
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long corpus_sink;

static volatile int seed_in = 5;
static volatile int at_in = 3;
static volatile int other_in = 6;

int main(void) {
    int seed = seed_in;
    int at = at_in;
    int other = other_in;

    int buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (int)i;
    }
    int value = buffer[at];
    buffer[other] = seed;
    int total = seed + value;
    corpus_sink = (long long)(buffer[0]);

    printf("%lld\n", (long long)(total));
    return 0;
}
