// a widened signed char widened in both arms and read narrow at the join
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile signed char a_in = 40;
static volatile signed char b_in = 15;
static volatile int flag_in = 1;

int main(void) {
    signed char a = a_in;
    signed char b = b_in;
    int flag = flag_in;

    int wide;
    if (flag) {
        wide = a;
    } else {
        wide = b;
    }
    signed char total = (signed char)wide;

    printf("%lld\n", (long long)(total));
    return 0;
}
