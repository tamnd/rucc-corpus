// a widened short whose only reader is in another block
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile short a_in = 40;
static volatile int flag_in = 1;

int main(void) {
    short a = a_in;
    int flag = flag_in;

    int wide = a;
    short low = (short)0;
    if (flag) {
        low = (short)wide;
    }
    short total = low;

    printf("%lld\n", (long long)(total));
    return 0;
}
