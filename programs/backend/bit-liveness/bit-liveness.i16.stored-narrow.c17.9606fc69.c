// a widened short whose only reader is a store at its own width
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile short a_in = 40;
static volatile int flag_in = 1;

int main(void) {
    short a = a_in;
    int flag = flag_in;

    short buffer[4] = { 0 };
    int wide = a;
    if (flag) {
        buffer[2] = (short)wide;
    }
    short total = buffer[2];

    printf("%lld\n", (long long)(total));
    return 0;
}
