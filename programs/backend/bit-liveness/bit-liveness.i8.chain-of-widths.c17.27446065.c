// a widened signed char widened twice and read back at its own width
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile signed char a_in = 40;
static volatile int flag_in = 1;

int main(void) {
    signed char a = a_in;
    int flag = flag_in;

    int wide = a;
    long long wider = wide;
    signed char low = (signed char)0;
    if (flag) {
        low = (signed char)wider;
    }
    signed char total = low;

    printf("%lld\n", (long long)(total));
    return 0;
}
