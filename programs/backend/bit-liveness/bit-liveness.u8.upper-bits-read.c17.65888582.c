// a widened unsigned char read whole as well as narrow
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned char a_in = 200u;
static volatile int flag_in = 1;

int main(void) {
    unsigned char a = a_in;
    int flag = flag_in;

    int wide = a;
    unsigned char low = (unsigned char)0u;
    int beyond = 0;
    if (flag) {
        low = (unsigned char)wide;
        beyond = wide > 127;
    }
    unsigned char total = (unsigned char)(low + (unsigned char)beyond);

    printf("%lld\n", (long long)(total));
    return 0;
}
