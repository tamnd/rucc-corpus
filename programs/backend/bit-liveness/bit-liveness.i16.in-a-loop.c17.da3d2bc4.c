// a widened short whose only reader is in a loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile short a_in = 5;
static volatile int count_in = 3;

int main(void) {
    short a = a_in;
    int count = count_in;

    int wide = a;
    short total = (short)0;
    for (int i = 0; i < count; i = i + 1) {
        total = (short)(total + (short)wide);
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
