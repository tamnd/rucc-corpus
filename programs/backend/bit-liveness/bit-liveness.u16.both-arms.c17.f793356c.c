// a widened unsigned short widened in both arms and read narrow at the join
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned short a_in = 40u;
static volatile unsigned short b_in = 15u;
static volatile int flag_in = 1;

int main(void) {
    unsigned short a = a_in;
    unsigned short b = b_in;
    int flag = flag_in;

    int wide;
    if (flag) {
        wide = a;
    } else {
        wide = b;
    }
    unsigned short total = (unsigned short)wide;

    printf("%lld\n", (long long)(total));
    return 0;
}
