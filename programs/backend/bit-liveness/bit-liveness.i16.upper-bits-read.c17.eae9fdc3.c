// a widened short read whole as well as narrow
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile short a_in = (-56);
static volatile int flag_in = 1;

int main(void) {
    short a = a_in;
    int flag = flag_in;

    int wide = a;
    short low = (short)0;
    int beyond = 0;
    if (flag) {
        low = (short)wide;
        beyond = wide < 0;
    }
    short total = (short)(low + (short)beyond);

    printf("%lld\n", (long long)(total));
    return 0;
}
