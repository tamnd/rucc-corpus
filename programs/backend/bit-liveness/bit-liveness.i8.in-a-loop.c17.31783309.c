// a widened signed char whose only reader is in a loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile signed char a_in = 5;
static volatile int count_in = 3;

int main(void) {
    signed char a = a_in;
    int count = count_in;

    int wide = a;
    signed char total = (signed char)0;
    for (int i = 0; i < count; i = i + 1) {
        total = (signed char)(total + (signed char)wide);
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
