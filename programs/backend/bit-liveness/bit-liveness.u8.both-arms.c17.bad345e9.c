// a widened unsigned char widened in both arms and read narrow at the join
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned char a_in = 40u;
static volatile unsigned char b_in = 15u;
static volatile int flag_in = 1;

int main(void) {
    unsigned char a = a_in;
    unsigned char b = b_in;
    int flag = flag_in;

    int wide;
    if (flag) {
        wide = a;
    } else {
        wide = b;
    }
    unsigned char total = (unsigned char)wide;

    printf("%lld\n", (long long)(total));
    return 0;
}
