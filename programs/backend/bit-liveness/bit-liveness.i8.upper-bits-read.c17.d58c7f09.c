// a widened signed char read whole as well as narrow
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile signed char a_in = (-56);
static volatile int flag_in = 1;

int main(void) {
    signed char a = a_in;
    int flag = flag_in;

    int wide = a;
    signed char low = (signed char)0;
    int beyond = 0;
    if (flag) {
        low = (signed char)wide;
        beyond = wide < 0;
    }
    signed char total = (signed char)(low + (signed char)beyond);

    printf("%lld\n", (long long)(total));
    return 0;
}
