// a two-writes memory access in a loop the compiler cannot unpick
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 1u;
static volatile int start_in = 0;
static volatile int gap_in = 0;

int main(void) {
    unsigned int seed = seed_in;
    int start = start_in;
    int gap = gap_in;

    unsigned int buffer[16];
    for (int i = 0; i < 16; i++) {
        buffer[i] = seed + (unsigned int)i;
    }
    unsigned int total = 0;
    for (int i = 0; i < 64; i++) {
        int at = (i * 7 + start) & 15;
        buffer[at] = total + (unsigned int)1u;
        buffer[(at + gap) & 15] = total + (unsigned int)2u;
        total = total * (unsigned int)5u + buffer[at];
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
