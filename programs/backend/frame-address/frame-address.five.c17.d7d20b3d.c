// one frame address, used at five offsets
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long room[5];
    room[0] = seed;
    room[1] = seed + (long long)1ll;
    room[2] = seed + (long long)2ll;
    room[3] = seed + (long long)3ll;
    room[4] = seed + (long long)4ll;
    long long total = room[0] + room[1] + room[2] + room[3] + room[4];

    printf("%lld\n", (long long)(total));
    return 0;
}
