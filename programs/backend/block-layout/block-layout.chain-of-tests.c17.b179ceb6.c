// a chain-of-tests branch inside a hot loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int rare_in = 0;

int main(void) {
    int rare = rare_in;

    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        if (rare == 1) {
            total += 1000;
        } else if (rare == 2) {
            total += 2000;
        } else if (rare == 3) {
            total += 3000;
        } else {
            total += 1;
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
