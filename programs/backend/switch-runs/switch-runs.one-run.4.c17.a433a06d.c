// one run of 4 labels sharing an arm
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int in_run(int value) {
    switch (value) {
    case 10:
    case 11:
    case 12:
    case 13:
        return 1;
    default:
        return 0;
    }
}
static volatile int first_in = 7;
static volatile int count_in = 10;

int main(void) {
    int first = first_in;
    int count = count_in;

    long long total = 0;
    for (int step = 0; step < count; step++) {
        total += in_run(first + step);
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
