// a struct of 4 fields passed by value
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

struct bag { long long f0; long long f1; long long f2; long long f3; };
static long long total(struct bag value) {
    return value.f0 + value.f1 + value.f2 + value.f3;
}
static struct bag build(long long seed) {
    struct bag out;
    out.f0 = seed + 0;
    out.f1 = seed + 1;
    out.f2 = seed + 2;
    out.f3 = seed + 3;
    return out;
}
static volatile long long seed_in = 1ll;

int main(void) {
    long long seed = seed_in;

    struct bag made = build(seed);
    printf("%lld\n", (long long)(total(made)));
    return 0;
}
