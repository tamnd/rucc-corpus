// arms that both add a widened masked int, differing only in what is masked
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 7u;
static volatile int mask_in = 85;

int main(void) {
    unsigned int seed = seed_in;
    int mask = mask_in;

    unsigned state = seed;
    long long total = 0;
    for (int i = 0; i < 20000000; i++) {
        state = state * 1103515245u + 12345u;
        int x = (int)(state >> 24);
        if ((state >> 16) % 100u < 50u) {
            total += (long long)((x * 3) ^ mask);
        } else {
            total += (long long)((x + 7) ^ mask);
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
