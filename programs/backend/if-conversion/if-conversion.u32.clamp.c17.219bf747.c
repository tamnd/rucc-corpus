// a clamp on unsigned int with an unpredictable condition
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int a_in = 20u;
static volatile unsigned int b_in = 12u;

int main(void) {
    unsigned int a = a_in;
    unsigned int b = b_in;

    unsigned int total = 0;
    for (int i = 0; i < 1000; i++) {
        int odd = i & 1;
        unsigned int raw = odd ? a : b;
        unsigned int low = raw < (unsigned int)15u ? (unsigned int)15u : raw;
        total += low > (unsigned int)18u ? (unsigned int)18u : low;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
