// a clamp on long long with an unpredictable condition
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 20ll;
static volatile long long b_in = 12ll;

int main(void) {
    long long a = a_in;
    long long b = b_in;

    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        int odd = i & 1;
        long long raw = odd ? a : b;
        long long low = raw < (long long)15ll ? (long long)15ll : raw;
        total += low > (long long)18ll ? (long long)18ll : low;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
