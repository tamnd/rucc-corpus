// one unsigned short read and written against a constant, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned short seed_in = 5u;
static unsigned short *volatile hold;

int main(void) {
    unsigned short seed = seed_in;

    unsigned short one = seed;
    unsigned short two = seed + (unsigned short)1u;
    hold = &one;
    hold = &two;
    two = one + 5;
    unsigned short total = one + two;

    printf("%lld\n", (long long)(total));
    return 0;
}
