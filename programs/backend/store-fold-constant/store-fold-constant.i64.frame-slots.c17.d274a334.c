// one long long read and written against a constant, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;
static long long *volatile hold;

int main(void) {
    long long seed = seed_in;

    long long one = seed;
    long long two = seed + (long long)1ll;
    hold = &one;
    hold = &two;
    two = one + 5;
    long long total = one + two;

    printf("%lld\n", (long long)(total));
    return 0;
}
