// one unsigned long long read and written against a constant, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static unsigned long long *volatile hold;

int main(void) {
    unsigned long long seed = seed_in;

    unsigned long long one = seed;
    unsigned long long two = seed + (unsigned long long)1ull;
    hold = &one;
    hold = &two;
    two = one + 5;
    unsigned long long total = one + two;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
