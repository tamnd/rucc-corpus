// one unsigned char read and written against a constant, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned char seed_in = 5u;
static unsigned char *volatile hold;

int main(void) {
    unsigned char seed = seed_in;

    unsigned char one = seed;
    unsigned char two = seed + (unsigned char)1u;
    hold = &one;
    hold = &two;
    two = one + 5;
    unsigned char total = one + two;

    printf("%lld\n", (long long)(total));
    return 0;
}
