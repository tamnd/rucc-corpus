// one signed char read and written against a constant, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile signed char seed_in = 5;
static signed char *volatile hold;

int main(void) {
    signed char seed = seed_in;

    signed char one = seed;
    signed char two = seed + (signed char)1;
    hold = &one;
    hold = &two;
    two = one + 5;
    signed char total = one + two;

    printf("%lld\n", (long long)(total));
    return 0;
}
