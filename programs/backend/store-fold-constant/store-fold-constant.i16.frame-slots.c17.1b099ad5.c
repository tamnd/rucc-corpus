// one short read and written against a constant, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile short seed_in = 5;
static short *volatile hold;

int main(void) {
    short seed = seed_in;

    short one = seed;
    short two = seed + (short)1;
    hold = &one;
    hold = &two;
    two = one + 5;
    short total = one + two;

    printf("%lld\n", (long long)(total));
    return 0;
}
