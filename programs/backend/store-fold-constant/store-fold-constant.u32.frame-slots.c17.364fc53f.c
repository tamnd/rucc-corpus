// one unsigned int read and written against a constant, frame-slots
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 5u;
static unsigned int *volatile hold;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int one = seed;
    unsigned int two = seed + (unsigned int)1u;
    hold = &one;
    hold = &two;
    two = one + 5;
    unsigned int total = one + two;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
