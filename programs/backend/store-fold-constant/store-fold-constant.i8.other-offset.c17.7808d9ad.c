// one signed char read and written against a constant, other-offset
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile signed char seed_in = 5;
static volatile int at_in = 3;
static volatile int back_in = 3;

int main(void) {
    signed char seed = seed_in;
    int at = at_in;
    int back = back_in;

    signed char buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (signed char)i;
    }
    buffer[at + 1] = buffer[at] + 5;
    signed char total = buffer[back] + buffer[back + 1];

    printf("%lld\n", (long long)(total));
    return 0;
}
