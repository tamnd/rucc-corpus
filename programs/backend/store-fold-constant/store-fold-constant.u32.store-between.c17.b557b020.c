// one unsigned int read and written against a constant, store-between
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 5u;
static volatile int at_in = 3;
static volatile int back_in = 3;
static volatile int other_in = 6;

int main(void) {
    unsigned int seed = seed_in;
    int at = at_in;
    int back = back_in;
    int other = other_in;

    unsigned int buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned int)i;
    }
    unsigned int value = buffer[at];
    buffer[other] = seed;
    buffer[at] = value + 5;
    unsigned int total = buffer[back] + buffer[other];

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
