// one unsigned long long read and written against a constant, other-place
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static volatile int at_in = 3;
static volatile int back_in = 3;
static volatile int other_in = 6;

int main(void) {
    unsigned long long seed = seed_in;
    int at = at_in;
    int back = back_in;
    int other = other_in;

    unsigned long long buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned long long)i;
    }
    buffer[other] = buffer[at] + 5;
    unsigned long long total = buffer[back] + buffer[other];

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
