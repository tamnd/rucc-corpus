// one unsigned char read and written against a constant, store-between
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned char seed_in = 5u;
static volatile int at_in = 3;
static volatile int back_in = 3;
static volatile int other_in = 6;

int main(void) {
    unsigned char seed = seed_in;
    int at = at_in;
    int back = back_in;
    int other = other_in;

    unsigned char buffer[8];
    for (int i = 0; i < 8; i++) {
        buffer[i] = seed + (unsigned char)i;
    }
    unsigned char value = buffer[at];
    buffer[other] = seed;
    buffer[at] = value + 5;
    unsigned char total = buffer[back] + buffer[other];

    printf("%lld\n", (long long)(total));
    return 0;
}
