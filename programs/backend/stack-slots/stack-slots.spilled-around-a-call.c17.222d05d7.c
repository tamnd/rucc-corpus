// two runs of frame bytes, spilled-around-a-call
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long opaque(long long v) {
    return v + 1;
}
static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    {
        long long room[8];
        for (int i = 0; i < 8; i++) {
            room[i] = seed + i;
        }
        for (int i = 0; i < 8; i++) {
            total += room[i];
        }
    }

    long long v0 = seed + 0;
    long long v1 = seed + 1;
    long long v2 = seed + 2;
    long long v3 = seed + 3;
    long long v4 = seed + 4;
    long long v5 = seed + 5;
    long long v6 = seed + 6;
    long long v7 = seed + 7;
    long long v8 = seed + 8;
    long long v9 = seed + 9;
    long long v10 = seed + 10;
    long long v11 = seed + 11;
    long long kept = opaque(seed);
    total += v0 + v1 + v2 + v3 + v4 + v5 + v6 + v7 + v8 + v9 + v10 + v11 + kept;

    printf("%lld\n", (long long)(total));
    return 0;
}
