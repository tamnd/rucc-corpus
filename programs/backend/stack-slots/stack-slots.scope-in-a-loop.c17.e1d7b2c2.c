// two runs of frame bytes, scope-in-a-loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    for (int k = 0; k < 4; k++) {
        long long room[8];
        for (int i = 0; i < 8; i++) {
            room[i] = seed + i + k;
        }
        for (int i = 0; i < 8; i++) {
            total += room[i];
        }
    }
    {
        long long after[8];
        for (int i = 0; i < 8; i++) {
            after[i] = seed + 10 + i;
        }
        for (int i = 0; i < 8; i++) {
            total += after[i];
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
