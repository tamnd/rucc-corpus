// two runs of frame bytes, wide-alignment
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    {
        long double wide[8];
        for (int i = 0; i < 8; i++) {
            wide[i] = (long double)(seed + i);
        }
        for (int i = 0; i < 8; i++) {
            total += (long long)wide[i];
        }
    }
    {
        long long narrow[8];
        for (int i = 0; i < 8; i++) {
            narrow[i] = seed + 10 + i;
        }
        for (int i = 0; i < 8; i++) {
            total += narrow[i];
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
