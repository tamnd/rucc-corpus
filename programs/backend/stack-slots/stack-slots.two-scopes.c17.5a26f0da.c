// two runs of frame bytes, two-scopes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    {
        long long first[8];
        for (int i = 0; i < 8; i++) {
            first[i] = seed + i;
        }
        for (int i = 0; i < 8; i++) {
            total += first[i];
        }
    }
    {
        long long second[8];
        for (int i = 0; i < 8; i++) {
            second[i] = seed + 10 + i;
        }
        for (int i = 0; i < 8; i++) {
            total += second[i];
        }
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
