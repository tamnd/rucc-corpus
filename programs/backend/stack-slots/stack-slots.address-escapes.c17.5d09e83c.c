// two runs of frame bytes, address-escapes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long *escaped;
static void stash(long long *p) {
    escaped = p;
}
static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    long long kept[8];
    for (int i = 0; i < 8; i++) {
        kept[i] = seed + i;
    }
    stash(kept);
    {
        long long other[8];
        for (int i = 0; i < 8; i++) {
            other[i] = seed + 10 + i;
        }
        for (int i = 0; i < 8; i++) {
            total += other[i];
        }
    }
    for (int i = 0; i < 8; i++) {
        total += escaped[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
