// 12 live int values held call-off-the-path, which is what the pressure model counts
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int opaque(int v) {
    return v + 1;
}
static volatile int seed_in = 1;
static volatile int flag_in = 1;

int main(void) {
    int seed = seed_in;
    int flag = flag_in;

    int v0 = seed + (int)0;
    int v1 = seed + (int)1;
    int v2 = seed + (int)2;
    int v3 = seed + (int)3;
    int v4 = seed + (int)4;
    int v5 = seed + (int)5;
    int v6 = seed + (int)6;
    int v7 = seed + (int)7;
    int v8 = seed + (int)8;
    int v9 = seed + (int)9;
    int v10 = seed + (int)10;
    int v11 = seed + (int)11;

    int kept = 0;
    if (flag == 0) {
        kept = opaque(seed);
    } else {
        kept += v11;
        kept += v10;
        kept += v9;
        kept += v8;
        kept += v7;
        kept += v6;
        kept += v5;
        kept += v4;
        kept += v3;
        kept += v2;
        kept += v1;
        kept += v0;
    }

    int total = kept;

    printf("%lld\n", (long long)(total));
    return 0;
}
