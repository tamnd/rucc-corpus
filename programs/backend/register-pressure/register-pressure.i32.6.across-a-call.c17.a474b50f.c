// 6 live int values held across-a-call, which is what the pressure model counts
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int opaque(int v) {
    return v + 1;
}
static volatile int seed_in = 1;
static volatile int flag_in = 1;

int main(void) {
    int seed = seed_in;
    int flag = flag_in;

    int v0 = seed + (int)0;
    int v1 = seed + (int)1;
    int v2 = seed + (int)2;
    int v3 = seed + (int)3;
    int v4 = seed + (int)4;
    int v5 = seed + (int)5;

    int kept = opaque(seed);

    int total = kept;
    total += v5;
    total += v4;
    total += v3;
    total += v2;
    total += v1;
    total += v0;

    printf("%lld\n", (long long)(total));
    return 0;
}
