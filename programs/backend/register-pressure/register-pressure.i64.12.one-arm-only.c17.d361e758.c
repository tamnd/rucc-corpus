// 12 live long long values held one-arm-only, which is what the pressure model counts
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 1ll;
static volatile int flag_in = 1;

int main(void) {
    long long seed = seed_in;
    int flag = flag_in;

    long long v0 = seed + (long long)0ll;
    long long v1 = seed + (long long)1ll;
    long long v2 = seed + (long long)2ll;
    long long v3 = seed + (long long)3ll;
    long long v4 = seed + (long long)4ll;
    long long v5 = seed + (long long)5ll;
    long long v6 = seed + (long long)6ll;
    long long v7 = seed + (long long)7ll;
    long long v8 = seed + (long long)8ll;
    long long v9 = seed + (long long)9ll;
    long long v10 = seed + (long long)10ll;
    long long v11 = seed + (long long)11ll;

    long long kept = 0;
    if (flag) {
        kept = (long long)1;
    } else {
        kept = (long long)2;
    }

    long long total = kept;
    total += v11;
    total += v10;
    total += v9;
    total += v8;
    total += v7;
    total += v6;
    total += v5;
    total += v4;
    total += v3;
    total += v2;
    total += v1;
    total += v0;

    printf("%lld\n", (long long)(total));
    return 0;
}
