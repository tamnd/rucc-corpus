// union-pun references to int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;
static volatile int index_in = 1;
union both { int whole; unsigned char parts[4]; };

int main(void) {
    int seed = seed_in;
    int index = index_in;

    union both holder;
    holder.whole = seed + (int)1;
    for (int i = 0; i < 4; i++) {
        holder.parts[i] = 0;
    }
    int total = holder.whole + (int)7;

    printf("%lld\n", (long long)(total));
    return 0;
}
