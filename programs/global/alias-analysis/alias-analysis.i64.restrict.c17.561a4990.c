// two long long references, restrict
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;
static long long combine(long long *restrict left, long long *restrict right) {
    *left += 1;
    *right += 2;
    return *left + *right;
}

int main(void) {
    long long seed = seed_in;

    long long left = seed, right = seed;
    long long total = combine(&left, &right);

    printf("%lld\n", (long long)(total));
    return 0;
}
