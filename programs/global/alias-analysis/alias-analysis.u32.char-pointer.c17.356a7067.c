// char-pointer references to unsigned int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 5u;
static volatile int index_in = 1;

int main(void) {
    unsigned int seed = seed_in;
    int index = index_in;

    unsigned int object = seed;
    unsigned int *at = &object;
    unsigned char *bytes = (unsigned char *)&object;
    *at = seed + (unsigned int)1u;
    for (int i = 0; i < 4; i++) {
        bytes[i] = 0;
    }
    unsigned int total = *at + (unsigned int)7u;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
