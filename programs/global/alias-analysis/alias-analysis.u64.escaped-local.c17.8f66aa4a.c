// escaped-local references to unsigned long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static volatile int index_in = 1;
static unsigned long long *kept;
static void keep(unsigned long long *of) {
    kept = of;
}
static void overwrite(void) {
    *kept = *kept + 2;
}

int main(void) {
    unsigned long long seed = seed_in;
    int index = index_in;

    unsigned long long local = seed;
    keep(&local);
    local = seed + (unsigned long long)1ull;
    overwrite();
    unsigned long long total = local;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
