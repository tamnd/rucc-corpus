// two int references, distinct-indices
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;

int main(void) {
    int seed = seed_in;

    int buffer[4] = { 0 };
    buffer[0] = seed + (int)1;
    buffer[3] = seed + (int)2;
    int total = buffer[0] + buffer[3];

    printf("%lld\n", (long long)(total));
    return 0;
}
