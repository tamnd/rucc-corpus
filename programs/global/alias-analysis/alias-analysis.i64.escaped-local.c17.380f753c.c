// escaped-local references to long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;
static volatile int index_in = 1;
static long long *kept;
static void keep(long long *of) {
    kept = of;
}
static void overwrite(void) {
    *kept = *kept + 2;
}

int main(void) {
    long long seed = seed_in;
    int index = index_in;

    long long local = seed;
    keep(&local);
    local = seed + (long long)1ll;
    overwrite();
    long long total = local;

    printf("%lld\n", (long long)(total));
    return 0;
}
