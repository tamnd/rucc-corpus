// union-pun references to unsigned long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long seed_in = 5ull;
static volatile int index_in = 1;
union both { unsigned long long whole; unsigned char parts[8]; };

int main(void) {
    unsigned long long seed = seed_in;
    int index = index_in;

    union both holder;
    holder.whole = seed + (unsigned long long)1ull;
    for (int i = 0; i < 8; i++) {
        holder.parts[i] = 0;
    }
    unsigned long long total = holder.whole + (unsigned long long)7ull;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
