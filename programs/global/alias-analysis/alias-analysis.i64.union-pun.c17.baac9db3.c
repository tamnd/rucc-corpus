// union-pun references to long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;
static volatile int index_in = 1;
union both { long long whole; unsigned char parts[8]; };

int main(void) {
    long long seed = seed_in;
    int index = index_in;

    union both holder;
    holder.whole = seed + (long long)1ll;
    for (int i = 0; i < 8; i++) {
        holder.parts[i] = 0;
    }
    long long total = holder.whole + (long long)7ll;

    printf("%lld\n", (long long)(total));
    return 0;
}
