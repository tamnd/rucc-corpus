// two long long references, distinct-indices
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long buffer[4] = { 0 };
    buffer[0] = seed + (long long)1ll;
    buffer[3] = seed + (long long)2ll;
    long long total = buffer[0] + buffer[3];

    printf("%lld\n", (long long)(total));
    return 0;
}
