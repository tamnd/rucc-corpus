// escaped-local references to unsigned int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 5u;
static volatile int index_in = 1;
static unsigned int *kept;
static void keep(unsigned int *of) {
    kept = of;
}
static void overwrite(void) {
    *kept = *kept + 2;
}

int main(void) {
    unsigned int seed = seed_in;
    int index = index_in;

    unsigned int local = seed;
    keep(&local);
    local = seed + (unsigned int)1u;
    overwrite();
    unsigned int total = local;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
