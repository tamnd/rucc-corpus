// a test inside a loop that the loop bound settles
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }

    int outer = 0;
    int inner = 0;
    for (int i = 0; i < 256; i++) {
        int x = data[i];
        int n = x & 15;
        for (int j = 0; j < n; j++) {
            outer++;
            if (j < 16) {
                inner++;
            }
        }
    }

    printf("%lld\n", (long long)(outer));
    printf("%lld\n", (long long)(inner));
    return 0;
}
