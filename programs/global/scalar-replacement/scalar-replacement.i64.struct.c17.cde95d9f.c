// a non-escaping long long aggregate, struct
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 8ll;
struct pair { long long first; long long second; };

int main(void) {
    long long seed = seed_in;

    struct pair p;
    p.first = seed;
    p.second = seed + (long long)1ll;
    long long total = p.first + p.second;

    printf("%lld\n", (long long)(total));
    return 0;
}
