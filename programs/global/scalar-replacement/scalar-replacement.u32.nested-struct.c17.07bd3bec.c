// a non-escaping unsigned int aggregate, nested-struct
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 8u;
struct inner { unsigned int value; };
struct outer { struct inner one; struct inner two; };

int main(void) {
    unsigned int seed = seed_in;

    struct outer o;
    o.one.value = seed;
    o.two.value = seed + (unsigned int)1u;
    unsigned int total = o.one.value + o.two.value;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
