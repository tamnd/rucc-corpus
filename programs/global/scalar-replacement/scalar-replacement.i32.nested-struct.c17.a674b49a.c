// a non-escaping int aggregate, nested-struct
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 8;
struct inner { int value; };
struct outer { struct inner one; struct inner two; };

int main(void) {
    int seed = seed_in;

    struct outer o;
    o.one.value = seed;
    o.two.value = seed + (int)1;
    int total = o.one.value + o.two.value;

    printf("%lld\n", (long long)(total));
    return 0;
}
