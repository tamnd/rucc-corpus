// a non-escaping int aggregate, union
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 8;
union holder { int value; int again; };

int main(void) {
    int seed = seed_in;

    union holder u;
    u.value = seed;
    int total = u.again + seed + (int)1;

    printf("%lld\n", (long long)(total));
    return 0;
}
