// a non-escaping long long aggregate, nested-struct
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 8ll;
struct inner { long long value; };
struct outer { struct inner one; struct inner two; };

int main(void) {
    long long seed = seed_in;

    struct outer o;
    o.one.value = seed;
    o.two.value = seed + (long long)1ll;
    long long total = o.one.value + o.two.value;

    printf("%lld\n", (long long)(total));
    return 0;
}
