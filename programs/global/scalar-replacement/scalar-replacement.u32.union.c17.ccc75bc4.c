// a non-escaping unsigned int aggregate, union
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned int seed_in = 8u;
union holder { unsigned int value; unsigned int again; };

int main(void) {
    unsigned int seed = seed_in;

    union holder u;
    u.value = seed;
    unsigned int total = u.again + seed + (unsigned int)1u;

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
