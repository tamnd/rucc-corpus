// a non-escaping long long aggregate, union
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 8ll;
union holder { long long value; long long again; };

int main(void) {
    long long seed = seed_in;

    union holder u;
    u.value = seed;
    long long total = u.again + seed + (long long)1ll;

    printf("%lld\n", (long long)(total));
    return 0;
}
