// sink-into-one-arm for long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 4ll;
static volatile long long b_in = 9ll;
static volatile int flag_in = 0;

int main(void) {
    long long a = a_in;
    long long b = b_in;
    int flag = flag_in;

    long long product = a * b;
    long long total;
    if (flag) {
        total = product + (long long)2ll;
    } else {
        total = product + (long long)1ll;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
