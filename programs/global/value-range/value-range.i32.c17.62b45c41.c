// questions a guard on a int already answered
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int raw_in = 40;

int main(void) {
    int raw = raw_in;

    int bounded = raw % (int)100;
    int masked = raw & (int)63;

    printf("%lld\n", (long long)(bounded < (int)100));
    printf("%lld\n", (long long)(masked < (int)64));
    printf("%lld\n", (long long)(masked >= (int)0));

    int guarded;
    if (raw > (int)10) {
        guarded = raw > (int)5;
    } else {
        guarded = raw <= (int)10;
    }
    printf("%lld\n", (long long)(guarded));
    int halved;
    if (raw >= (int)0) {
        halved = (raw / (int)2) >= (int)0;
    } else {
        halved = (raw / (int)2) <= (int)0;
    }
    printf("%lld\n", (long long)(halved));
    return 0;
}
