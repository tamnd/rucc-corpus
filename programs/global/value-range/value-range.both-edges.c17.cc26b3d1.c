// a range that comes from both-edges
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int x_in = 20;

int main(void) {
    int x = x_in;

    int settled;
    if (x > 10) {
        settled = (x > 5) && (x >= 11);
    } else {
        settled = (x <= 10) && (x < 11);
    }

    printf("%lld\n", (long long)(settled));
    return 0;
}
