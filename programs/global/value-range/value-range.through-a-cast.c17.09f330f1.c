// a range that comes from through-a-cast
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int x_in = 1000;

int main(void) {
    int x = x_in;

    unsigned char narrow = (unsigned char)x;
    int wide = (int)narrow;
    signed char signed_narrow = (signed char)(x & 63);
    int signed_wide = (int)signed_narrow;

    printf("%lld\n", (long long)(wide >= 0 && wide <= 255));
    printf("%lld\n", (long long)(signed_wide >= 0 && signed_wide <= 63));
    return 0;
}
