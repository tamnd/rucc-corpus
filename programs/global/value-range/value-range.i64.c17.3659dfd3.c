// questions a guard on a long long already answered
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long raw_in = 40ll;

int main(void) {
    long long raw = raw_in;

    long long bounded = raw % (long long)100ll;
    long long masked = raw & (long long)63ll;

    printf("%lld\n", (long long)(bounded < (long long)100ll));
    printf("%lld\n", (long long)(masked < (long long)64ll));
    printf("%lld\n", (long long)(masked >= (long long)0ll));

    int guarded;
    if (raw > (long long)10ll) {
        guarded = raw > (long long)5ll;
    } else {
        guarded = raw <= (long long)10ll;
    }
    printf("%lld\n", (long long)(guarded));
    int halved;
    if (raw >= (long long)0ll) {
        halved = (raw / (long long)2ll) >= (long long)0ll;
    } else {
        halved = (raw / (long long)2ll) <= (long long)0ll;
    }
    printf("%lld\n", (long long)(halved));
    return 0;
}
