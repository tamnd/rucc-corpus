// a range that comes from relation-between-two
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int a_in = 3;
static volatile int b_in = 8;
static volatile int c_in = 11;

int main(void) {
    int a = a_in;
    int b = b_in;
    int c = c_in;

    int settled = 1;
    if (a < b) {
        if (b < c) {
            settled = (a < c) && (a != c) && (c > a);
        }
    }

    printf("%lld\n", (long long)(settled));
    return 0;
}
