// a range that comes from guard-keeps-the-index
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int index_in = 4;

int main(void) {
    int index = index_in;

    int table[4] = { 10, 20, 30, 40 };
    int picked;
    if (index >= 0 && index < 4) {
        picked = table[index];
    } else {
        picked = 9;
    }

    printf("%lld\n", (long long)(picked == 9));
    return 0;
}
