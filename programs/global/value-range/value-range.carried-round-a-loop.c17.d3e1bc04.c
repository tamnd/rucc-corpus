// a range that comes from carried-round-a-loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int limit_in = 10;

int main(void) {
    int limit = limit_in;

    int inside = 1;
    int i = 0;
    for (i = 0; i < limit; i++) {
        inside = inside && (i >= 0) && (i < limit);
    }

    printf("%lld\n", (long long)(inside));
    printf("%lld\n", (long long)(i >= limit));
    return 0;
}
