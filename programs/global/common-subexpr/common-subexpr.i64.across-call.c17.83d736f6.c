// a repeated long long expression, across-call
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 6ll;
static volatile long long b_in = 7ll;
static volatile int flag_in = 1;
static long long identity(long long value) {
    return value;
}

int main(void) {
    long long a = a_in;
    long long b = b_in;
    int flag = flag_in;

    long long first = a * b;
    long long kept = identity(first);
    long long total = kept + a * b;

    printf("%lld\n", (long long)(total));
    return 0;
}
