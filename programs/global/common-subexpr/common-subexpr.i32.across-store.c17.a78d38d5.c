// a repeated int expression, across-store
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long corpus_sink;

static volatile int a_in = 6;
static volatile int b_in = 7;
static volatile int flag_in = 1;

int main(void) {
    int a = a_in;
    int b = b_in;
    int flag = flag_in;

    int slots[2];
    int first = a * b;
    slots[0] = a;
    slots[1] = b;
    int second = a * b;
    int total = first + second;
    corpus_sink = (long long)(slots[0] + slots[1]);

    printf("%lld\n", (long long)(total));
    return 0;
}
