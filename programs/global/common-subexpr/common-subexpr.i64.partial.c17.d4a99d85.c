// a repeated long long expression, partial
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long a_in = 6ll;
static volatile long long b_in = 7ll;
static volatile int flag_in = 1;

int main(void) {
    long long a = a_in;
    long long b = b_in;
    int flag = flag_in;

    long long total = a * b;
    if (flag) {
        total = total + a * b;
    } else {
        total = total + (long long)0ll;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
