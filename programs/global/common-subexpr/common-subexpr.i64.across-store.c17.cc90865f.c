// a repeated long long expression, across-store
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long corpus_sink;

static volatile long long a_in = 6ll;
static volatile long long b_in = 7ll;
static volatile int flag_in = 1;

int main(void) {
    long long a = a_in;
    long long b = b_in;
    int flag = flag_in;

    long long slots[2];
    long long first = a * b;
    slots[0] = a;
    slots[1] = b;
    long long second = a * b;
    long long total = first + second;
    corpus_sink = (long long)(slots[0] + slots[1]);

    printf("%lld\n", (long long)(total));
    return 0;
}
