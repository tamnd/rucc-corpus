// a repeated unsigned long long expression, partial
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile unsigned long long a_in = 6ull;
static volatile unsigned long long b_in = 7ull;
static volatile int flag_in = 1;

int main(void) {
    unsigned long long a = a_in;
    unsigned long long b = b_in;
    int flag = flag_in;

    unsigned long long total = a * b;
    if (flag) {
        total = total + a * b;
    } else {
        total = total + (unsigned long long)0ull;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
