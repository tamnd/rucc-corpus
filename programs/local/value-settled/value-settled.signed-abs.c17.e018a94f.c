// a signed abs, which is not the identity and must keep both arms
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }

    long long total = 0;
    for (int i = 0; i < 256; i++) {
        int a = data[i];
        int x = a - 128;
        total += x > 0 ? x : 0 - x;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
