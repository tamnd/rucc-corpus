// an unsigned abs, which is the identity and should fold to nothing
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }

    long long total = 0;
    for (int i = 0; i < 256; i++) {
        int a = data[i];
        unsigned int x = a;
        total += x > 0u ? x : 0u - x;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
