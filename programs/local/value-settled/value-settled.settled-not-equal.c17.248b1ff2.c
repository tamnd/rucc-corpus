// an inequality that settles the value on the arm it is false on
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    int mate[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
        mate[i] = (i % 4) == 0 ? data[i] : ((data[i] + 1) & 255);
    }

    long long total = 0;
    for (int i = 0; i < 256; i++) {
        int a = data[i];
        int b = mate[i];
        total += a != b ? a : b;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
