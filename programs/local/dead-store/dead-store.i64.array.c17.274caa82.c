// overwritten array stores of long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile long long seed_in = 5ll;

int main(void) {
    long long seed = seed_in;

    long long buffer[4];
    buffer[0] = seed;
    buffer[0] = seed + (long long)1ll;
    buffer[0] = seed + (long long)3ll;
    long long slot = buffer[0];

    printf("%lld\n", (long long)(slot));
    return 0;
}
