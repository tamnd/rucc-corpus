// overwritten array stores of int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;

int main(void) {
    int seed = seed_in;

    int buffer[4];
    buffer[0] = seed;
    buffer[0] = seed + (int)1;
    buffer[0] = seed + (int)3;
    int slot = buffer[0];

    printf("%lld\n", (long long)(slot));
    return 0;
}
