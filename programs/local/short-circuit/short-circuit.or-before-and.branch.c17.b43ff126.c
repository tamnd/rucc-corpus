// or written first with and still binding tighter, written as a branch
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }

    int hits = 0;
    for (int i = 0; i < 256; i++) {
        int v = data[i];
        if (v < 20 || v > 200 && (v & 1) == 0) {
            hits++;
        }
    }

    printf("%lld\n", (long long)(hits));
    return 0;
}
