// a right half that calls something with an effect, so it cannot move, written as a branch
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int calls;
static int bump(int v) {
    calls++;
    return (v & 8) != 0;
}
static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }

    int hits = 0;
    for (int i = 0; i < 256; i++) {
        int v = data[i];
        if ((v & 1) == 0 && bump(v)) {
            hits++;
        }
    }

    printf("%lld\n", (long long)(hits));
    printf("%lld\n", (long long)(calls));
    return 0;
}
