// a load that only the left half says is allowed, written as a value
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

struct cell { int x; };
static struct cell cells[256];
static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }
    for (int i = 0; i < 256; i++) {
        cells[i].x = data[i] & 4;
    }

    int hits = 0;
    for (int i = 0; i < 256; i++) {
        int v = data[i];
        struct cell *p = (v & 3) != 0 ? &cells[i] : 0;
        int got = p && p->x;
        hits += got;
    }

    printf("%lld\n", (long long)(hits));
    return 0;
}
