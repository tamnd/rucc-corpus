// an index that is only inside the array when the left half says so, written as a branch
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }
    int small[16];
    for (int i = 0; i < 16; i++) {
        small[i] = i & 1;
    }

    int hits = 0;
    for (int i = 0; i < 256; i++) {
        int v = data[i];
        int k = v & 31;
        if (k < 16 && small[k]) {
            hits++;
        }
    }

    printf("%lld\n", (long long)(hits));
    return 0;
}
