// two arms storing to two addresses that are not the same
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }
    int left[256];
    for (int i = 0; i < 256; i++) {
        left[i] = 5;
    }
    int right[256];
    for (int i = 0; i < 256; i++) {
        right[i] = 5;
    }

    for (int i = 0; i < 256; i++) {
        int v = data[i];
        if (v & 1) {
            left[i] = v;
        } else {
            right[i] = v + 1;
        }
    }

    long long total = 0;
    for (int i = 0; i < 256; i++) {
        total += left[i];
        total += right[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
