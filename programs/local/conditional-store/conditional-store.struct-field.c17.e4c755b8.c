// both arms storing to one field of one struct
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

struct box { int lo; int x; int hi; };
static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }
    struct box boxes[256];
    for (int i = 0; i < 256; i++) {
        boxes[i].lo = 0;
        boxes[i].hi = 0;
    }

    for (int i = 0; i < 256; i++) {
        int v = data[i];
        struct box *s = &boxes[i];
        if (v & 1) {
            s->x = v;
        } else {
            s->x = v + 1;
        }
    }

    long long total = 0;
    for (int i = 0; i < 256; i++) {
        total += boxes[i].lo;
        total += boxes[i].x;
        total += boxes[i].hi;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
