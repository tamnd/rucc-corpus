// both arms storing short to the same place
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }
    short out[256];

    for (int i = 0; i < 256; i++) {
        int v = data[i];
        short *q = &out[i];
        if (v & 4) {
            *q = (short)(v * 3);
        } else {
            *q = (short)(v + 100);
        }
    }

    long long total = 0;
    for (int i = 0; i < 256; i++) {
        total += out[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
