// both arms storing long long to the same place
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int data[256];
    for (int i = 0; i < 256; i++) {
        data[i] = (i * 37 + seed) & 255;
    }
    long long out[256];

    for (int i = 0; i < 256; i++) {
        int v = data[i];
        long long *q = &out[i];
        if (v & 4) {
            *q = (long long)(v * 3);
        } else {
            *q = (long long)(v + 100);
        }
    }

    long long total = 0;
    for (int i = 0; i < 256; i++) {
        total += out[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
