// a early-return whose dead arm must not run
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 10;
static int pick(int seed) {
    return seed + 1;
    return seed * 100;
}

int main(void) {
    int seed = seed_in;

    int answer = 0;
    answer = pick(seed);

    printf("%lld\n", (long long)(answer));
    return 0;
}
