// a dead-call-compare whose dead arm must not run
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 10;
void corpus_link_error(void);

int main(void) {
    int seed = seed_in;

    int answer = 0;
    answer = seed + 1;
    if (3 > 5) {
        corpus_link_error();
        answer = seed * 100;
    }

    printf("%lld\n", (long long)(answer));
    return 0;
}
