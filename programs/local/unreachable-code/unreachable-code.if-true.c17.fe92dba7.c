// a if-true whose dead arm must not run
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 10;

int main(void) {
    int seed = seed_in;

    int answer = 0;
    if (1) {
        answer = seed + 1;
    } else {
        answer = seed * 100;
    }

    printf("%lld\n", (long long)(answer));
    return 0;
}
