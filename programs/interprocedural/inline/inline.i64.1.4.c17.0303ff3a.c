// a 1 statement long long helper called from 4 places
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long helper(long long value) {
    value = value + (long long)1ll;
    return value;
}
static volatile long long seed_in = 0ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    total += helper(seed + (long long)0ll);
    total += helper(seed + (long long)1ll);
    total += helper(seed + (long long)2ll);
    total += helper(seed + (long long)3ll);

    printf("%lld\n", (long long)(total));
    return 0;
}
