// rotate dispatch between 1 functions in tail position, done many times
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static __attribute__((noinline)) unsigned long long rotate(const unsigned char *p, const unsigned char *end, unsigned long long              a, unsigned long long b, unsigned long long c, unsigned long long d) {
    if (p == end) {
        return a + 3 * b + 5 * c + 7 * d;
    }
    return rotate(p + 1, end, b, c, d, a + *p);
}
static volatile unsigned long long seed_in = 7ull;

int main(void) {
    unsigned long long total = 0;
    unsigned long long seed = seed_in;

    static unsigned char text[2048];
    unsigned long long drawn = seed;
    for (int i = 0; i < 2048; i++) {
        drawn = drawn * 6364136223846793005ull + 1442695040888963407ull;
        text[i] = (unsigned char)(drawn >> 33);
    }
    for (int round = 0; round < 1024; round++) {
        total += rotate(text, text + 2048, (unsigned long long)round, 1, 2, 3);
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
