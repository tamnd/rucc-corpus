// int callee, reads only
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int table[4];
static int watch[4];
static __attribute__((noinline)) int scan(int *in) {
    return in[0] + in[3];
}
static volatile int seed_in = 6;

int main(void) {
    int seed = seed_in;

    table[0] = seed;
    table[3] = seed + (int)1;
    watch[1] = seed + (int)2;
    watch[2] = seed + (int)3;
    int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += scan(table);
        total += watch[1] + watch[2];
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(table[0]));
    printf("%lld\n", (long long)(watch[1]));
    return 0;
}
