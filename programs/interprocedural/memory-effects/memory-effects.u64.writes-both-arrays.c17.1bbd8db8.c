// unsigned long long callee, writes both arrays
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned long long left[4];
static unsigned long long right[4];
static __attribute__((noinline)) void bump(unsigned long long *out, unsigned long long *in) {
    out[0] = out[0] + in[1];
    in[2] = in[2] + (unsigned long long)1ull;
}
static volatile unsigned long long seed_in = 6ull;

int main(void) {
    unsigned long long seed = seed_in;

    left[0] = seed;
    right[1] = seed + (unsigned long long)1ull;
    right[2] = seed + (unsigned long long)2ull;
    right[3] = seed + (unsigned long long)3ull;
    unsigned long long total = 0;
    for (int i = 0; i < 1000; i++) {
        bump(left, right);
        total += right[2] + right[3];
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(left[0]));
    printf("%llu\n", (unsigned long long)(right[2]));
    return 0;
}
