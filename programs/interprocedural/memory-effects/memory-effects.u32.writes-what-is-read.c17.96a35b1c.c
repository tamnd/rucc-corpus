// unsigned int callee, writes what is read
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int table[4];
static unsigned int watch[4];
static __attribute__((noinline)) unsigned int scan(unsigned int *in) {
    watch[1] = watch[1] + (unsigned int)1u;
    return in[0] + in[3];
}
static volatile unsigned int seed_in = 6u;

int main(void) {
    unsigned int seed = seed_in;

    table[0] = seed;
    table[3] = seed + (unsigned int)1u;
    watch[1] = seed + (unsigned int)2u;
    watch[2] = seed + (unsigned int)3u;
    unsigned int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += scan(table);
        total += watch[1] + watch[2];
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(table[0]));
    printf("%llu\n", (unsigned long long)(watch[1]));
    return 0;
}
