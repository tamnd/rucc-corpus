// long long callee, keeps a local
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long *kept;
static __attribute__((noinline)) long long sum_of(long long *of) {
    kept = of;
    return of[0] + of[1];
}
static __attribute__((noinline)) void tick(void) {
    *kept = *kept + (long long)1ll;
}
static volatile long long seed_in = 6ll;

int main(void) {
    long long seed = seed_in;

    long long local[3];
    local[0] = seed;
    local[1] = seed + (long long)1ll;
    local[2] = seed + (long long)2ll;
    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += sum_of(local);
        tick();
        total += local[0] + local[2];
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(local[0]));
    printf("%lld\n", (long long)(local[2]));
    return 0;
}
