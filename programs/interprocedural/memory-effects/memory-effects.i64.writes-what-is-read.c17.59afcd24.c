// long long callee, writes what is read
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long table[4];
static long long watch[4];
static __attribute__((noinline)) long long scan(long long *in) {
    watch[1] = watch[1] + (long long)1ll;
    return in[0] + in[3];
}
static volatile long long seed_in = 6ll;

int main(void) {
    long long seed = seed_in;

    table[0] = seed;
    table[3] = seed + (long long)1ll;
    watch[1] = seed + (long long)2ll;
    watch[2] = seed + (long long)3ll;
    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += scan(table);
        total += watch[1] + watch[2];
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(table[0]));
    printf("%lld\n", (long long)(watch[1]));
    return 0;
}
