// unsigned long long callee, lends a local
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned long long anchor;
static __attribute__((noinline)) unsigned long long sum_of(unsigned long long *of) {
    return of[0] + of[1];
}
static __attribute__((noinline)) void tick(void) {
    anchor = anchor + (unsigned long long)1ull;
}
static volatile unsigned long long seed_in = 6ull;

int main(void) {
    unsigned long long seed = seed_in;

    unsigned long long local[3];
    local[0] = seed;
    local[1] = seed + (unsigned long long)1ull;
    local[2] = seed + (unsigned long long)2ull;
    unsigned long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += sum_of(local);
        tick();
        total += local[0] + local[2];
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(local[0]));
    printf("%llu\n", (unsigned long long)(anchor));
    return 0;
}
