// int callee, keeps a local
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int *kept;
static __attribute__((noinline)) int sum_of(int *of) {
    kept = of;
    return of[0] + of[1];
}
static __attribute__((noinline)) void tick(void) {
    *kept = *kept + (int)1;
}
static volatile int seed_in = 6;

int main(void) {
    int seed = seed_in;

    int local[3];
    local[0] = seed;
    local[1] = seed + (int)1;
    local[2] = seed + (int)2;
    int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += sum_of(local);
        tick();
        total += local[0] + local[2];
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(local[0]));
    printf("%lld\n", (long long)(local[2]));
    return 0;
}
