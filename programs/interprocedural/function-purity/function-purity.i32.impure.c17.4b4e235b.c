// a impure int function called five times
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int calls = 0;
static int helper(int value) {
    calls = calls + 1;
    return (int)2 * value;
}
static volatile int seed_in = 6;

int main(void) {
    int seed = seed_in;

    int total = 0;
    for (int i = 0; i < 5; i++) {
        total += helper(seed);
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(calls));
    return 0;
}
