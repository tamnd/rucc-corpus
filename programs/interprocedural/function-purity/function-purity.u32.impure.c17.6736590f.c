// a impure unsigned int function called five times
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int calls = 0;
static unsigned int helper(unsigned int value) {
    calls = calls + 1;
    return (unsigned int)2u * value;
}
static volatile unsigned int seed_in = 6u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    for (int i = 0; i < 5; i++) {
        total += helper(seed);
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(calls));
    return 0;
}
