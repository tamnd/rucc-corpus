// a looping-const unsigned long long function called five times
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned long long helper(unsigned long long value) {
    unsigned long long sum = 0;
    for (unsigned long long i = 0; i < value; i++) {
        sum += (unsigned long long)2ull;
    }
    return sum;
}
static volatile unsigned long long seed_in = 6ull;

int main(void) {
    unsigned long long seed = seed_in;

    unsigned long long total = 0;
    for (int i = 0; i < 5; i++) {
        helper(seed);
        total += helper(seed);
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
