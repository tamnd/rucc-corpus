// a unsigned int return value no call reads, recursive
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int seen = (unsigned int)0u;
static unsigned int walk(unsigned int value) {
    seen = (unsigned int)(seen + value);
    if (value == (unsigned int)0u) {
        return (unsigned int)0u;
    }
    return walk((unsigned int)(value - 1));
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    walk(seed);

    printf("%llu\n", (unsigned long long)(seen));

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
