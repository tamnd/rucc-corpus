// a long long return value no call reads, recursive
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long seen = (long long)0ll;
static long long walk(long long value) {
    seen = (long long)(seen + value);
    if (value == (long long)0ll) {
        return (long long)0ll;
    }
    return walk((long long)(value - 1));
}
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    walk(seed);

    printf("%lld\n", (long long)(seen));

    printf("%lld\n", (long long)(total));
    return 0;
}
