// a int return value no call reads, other-objects-call-it
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int seen = (int)0;
int note(int value) {
    seen = (int)(seen + value);
    return (int)(value * value);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    note(seed);
    note((int)(seed + 1));

    printf("%lld\n", (long long)(seen));

    printf("%lld\n", (long long)(total));
    return 0;
}
