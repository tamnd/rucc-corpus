// a unsigned int return value no call reads, other-objects-call-it
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int seen = (unsigned int)0u;
unsigned int note(unsigned int value) {
    seen = (unsigned int)(seen + value);
    return (unsigned int)(value * value);
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    note(seed);
    note((unsigned int)(seed + 1));

    printf("%llu\n", (unsigned long long)(seen));

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
