// a int return value no call reads, recursive
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int seen = (int)0;
static int walk(int value) {
    seen = (int)(seen + value);
    if (value == (int)0) {
        return (int)0;
    }
    return walk((int)(value - 1));
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    walk(seed);

    printf("%lld\n", (long long)(seen));

    printf("%lld\n", (long long)(total));
    return 0;
}
