// long long call, reads a table nothing writes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long table[4];
static long long scratch[4];
static __attribute__((noinline)) long long scan(long long *in) {
    return in[0] + in[3];
}
static volatile long long seed_in = 6ll;

int main(void) {
    long long seed = seed_in;

    table[0] = seed;
    table[3] = seed + (long long)1ll;
    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += scan(table);
        scratch[i & 3] = scratch[i & 3] + (long long)1ll;
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(table[3]));
    printf("%lld\n", (long long)(scratch[3]));
    return 0;
}
