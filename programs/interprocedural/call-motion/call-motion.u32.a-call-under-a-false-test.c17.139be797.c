// unsigned int call, a call under a false test
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static __attribute__((noinline)) unsigned int divide(unsigned int top, unsigned int bottom) {
    return top / bottom;
}
static volatile unsigned int seed_in = 6u;
static volatile unsigned int gate_in = 0u;
static volatile unsigned int bottom_in = 0u;

int main(void) {
    unsigned int seed = seed_in;
    unsigned int gate = gate_in;
    unsigned int bottom = bottom_in;

    unsigned int total = 0;
    for (int i = 0; i < 1000; i++) {
        if (gate) {
            total += divide(seed, bottom);
        }
        total += (unsigned int)1u;
    }

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
