// unsigned int call in a loop, another unit is silent
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

unsigned int corpus_weigh(unsigned int of);
static unsigned int scratch[4];
static volatile unsigned int seed_in = 6u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += corpus_weigh(seed);
        scratch[i & 3] = scratch[i & 3] + (unsigned int)1u;
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(scratch[3]));
    return 0;
}
