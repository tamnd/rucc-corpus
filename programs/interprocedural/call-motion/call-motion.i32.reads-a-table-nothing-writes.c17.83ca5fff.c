// int call, reads a table nothing writes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int table[4];
static int scratch[4];
static __attribute__((noinline)) int scan(int *in) {
    return in[0] + in[3];
}
static volatile int seed_in = 6;

int main(void) {
    int seed = seed_in;

    table[0] = seed;
    table[3] = seed + (int)1;
    int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += scan(table);
        scratch[i & 3] = scratch[i & 3] + (int)1;
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(table[3]));
    printf("%lld\n", (long long)(scratch[3]));
    return 0;
}
