// int call, an argument the loop changes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int scratch[4];
static __attribute__((noinline)) int square(int of) {
    return of * of;
}
static volatile int seed_in = 6;

int main(void) {
    int seed = seed_in;

    int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += square(seed + (int)i);
        scratch[i & 3] = scratch[i & 3] + (int)1;
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(scratch[3]));
    return 0;
}
