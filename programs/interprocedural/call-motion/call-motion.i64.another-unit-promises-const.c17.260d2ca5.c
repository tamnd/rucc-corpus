// long long call in a loop, another unit promises const
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

__attribute__((const)) long long corpus_weigh(long long of);
static long long scratch[4];
static volatile long long seed_in = 6ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += corpus_weigh(seed);
        scratch[i & 3] = scratch[i & 3] + (long long)1ll;
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(scratch[3]));
    return 0;
}
