// unsigned int call in a loop, another unit writes a counter
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

unsigned int corpus_step(unsigned int of);
unsigned int corpus_calls(void);
static volatile unsigned int seed_in = 6u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += corpus_step(seed);
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(corpus_calls()));
    return 0;
}
