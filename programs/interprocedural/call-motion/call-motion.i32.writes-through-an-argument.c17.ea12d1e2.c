// int call, writes through an argument
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int counter[2];
static __attribute__((noinline)) int bump(int *out) {
    out[0] = out[0] + (int)1;
    return out[1];
}
static volatile int seed_in = 6;

int main(void) {
    int seed = seed_in;

    counter[1] = seed;
    int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += bump(counter);
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(counter[0]));
    printf("%lld\n", (long long)(counter[1]));
    return 0;
}
