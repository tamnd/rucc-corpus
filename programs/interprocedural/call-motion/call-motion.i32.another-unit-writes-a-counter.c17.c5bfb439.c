// int call in a loop, another unit writes a counter
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int corpus_step(int of);
int corpus_calls(void);
static volatile int seed_in = 6;

int main(void) {
    int seed = seed_in;

    int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += corpus_step(seed);
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(corpus_calls()));
    return 0;
}
