// long long call, an argument the loop changes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long scratch[4];
static __attribute__((noinline)) long long square(long long of) {
    return of * of;
}
static volatile long long seed_in = 6ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += square(seed + (long long)i);
        scratch[i & 3] = scratch[i & 3] + (long long)1ll;
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(scratch[3]));
    return 0;
}
