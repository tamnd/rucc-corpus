// long long call in a loop, another unit writes a counter
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

long long corpus_step(long long of);
long long corpus_calls(void);
static volatile long long seed_in = 6ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += corpus_step(seed);
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(corpus_calls()));
    return 0;
}
