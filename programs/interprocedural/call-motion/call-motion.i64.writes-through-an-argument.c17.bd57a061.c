// long long call, writes through an argument
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long counter[2];
static __attribute__((noinline)) long long bump(long long *out) {
    out[0] = out[0] + (long long)1ll;
    return out[1];
}
static volatile long long seed_in = 6ll;

int main(void) {
    long long seed = seed_in;

    counter[1] = seed;
    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += bump(counter);
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(counter[0]));
    printf("%lld\n", (long long)(counter[1]));
    return 0;
}
