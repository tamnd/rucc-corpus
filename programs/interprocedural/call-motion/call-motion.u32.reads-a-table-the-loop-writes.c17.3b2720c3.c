// unsigned int call, reads a table the loop writes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int table[4];
static __attribute__((noinline)) unsigned int scan(unsigned int *in) {
    return in[0] + in[3];
}
static volatile unsigned int seed_in = 6u;

int main(void) {
    unsigned int seed = seed_in;

    table[0] = seed;
    table[3] = seed + (unsigned int)1u;
    unsigned int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += scan(table);
        table[3] = table[3] + (unsigned int)1u;
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(table[0]));
    printf("%llu\n", (unsigned long long)(table[3]));
    return 0;
}
