// unsigned int call, an argument the loop changes
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int scratch[4];
static __attribute__((noinline)) unsigned int square(unsigned int of) {
    return of * of;
}
static volatile unsigned int seed_in = 6u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    for (int i = 0; i < 1000; i++) {
        total += square(seed + (unsigned int)i);
        scratch[i & 3] = scratch[i & 3] + (unsigned int)1u;
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(scratch[3]));
    return 0;
}
