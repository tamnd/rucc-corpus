// long long call, a call under a false test
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static __attribute__((noinline)) long long divide(long long top, long long bottom) {
    return top / bottom;
}
static volatile long long seed_in = 6ll;
static volatile long long gate_in = 0ll;
static volatile long long bottom_in = 0ll;

int main(void) {
    long long seed = seed_in;
    long long gate = gate_in;
    long long bottom = bottom_in;

    long long total = 0;
    for (int i = 0; i < 1000; i++) {
        if (gate) {
            total += divide(seed, bottom);
        }
        total += (long long)1ll;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
