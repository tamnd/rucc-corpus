// int call, a call under a false test
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static __attribute__((noinline)) int divide(int top, int bottom) {
    return top / bottom;
}
static volatile int seed_in = 6;
static volatile int gate_in = 0;
static volatile int bottom_in = 0;

int main(void) {
    int seed = seed_in;
    int gate = gate_in;
    int bottom = bottom_in;

    int total = 0;
    for (int i = 0; i < 1000; i++) {
        if (gate) {
            total += divide(seed, bottom);
        }
        total += (int)1;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
