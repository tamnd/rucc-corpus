// unsigned long long call in a loop, another unit writes a counter
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

unsigned long long corpus_step(unsigned long long of);
unsigned long long corpus_calls(void);
static volatile unsigned long long seed_in = 6ull;

int main(void) {
    unsigned long long seed = seed_in;

    unsigned long long total = 0;
    for (int i = 0; i < 1000; i++) {
        total += corpus_step(seed);
    }

    printf("%llu\n", (unsigned long long)(total));
    printf("%llu\n", (unsigned long long)(corpus_calls()));
    return 0;
}
