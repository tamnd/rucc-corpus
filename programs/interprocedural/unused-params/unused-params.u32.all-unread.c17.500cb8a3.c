// a unsigned int parameter nothing reads, all-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int fixed(unsigned int one, unsigned int two) {
    return (unsigned int)7u;
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    total += fixed(seed, (unsigned int)(seed + 1));
    total += fixed(seed, (unsigned int)(seed + 1));

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
