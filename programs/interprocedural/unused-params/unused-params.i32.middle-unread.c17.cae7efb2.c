// a int parameter nothing reads, middle-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int pick(int first, int ignored, int last) {
    return (int)(first * 10 + last);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += pick(seed, (int)(seed + 7), (int)(seed + 1));
    total += pick(seed, seed, (int)(seed + 2));

    printf("%lld\n", (long long)(total));
    return 0;
}
