// a int parameter nothing reads, constant-then-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int scaled(int value, int factor) {
    return (int)(value * factor);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += scaled(seed, (int)10);
    total += scaled(seed, (int)10);

    printf("%lld\n", (long long)(total));
    return 0;
}
