// a long long parameter nothing reads, middle-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long pick(long long first, long long ignored, long long last) {
    return (long long)(first * 10 + last);
}
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    total += pick(seed, (long long)(seed + 7), (long long)(seed + 1));
    total += pick(seed, seed, (long long)(seed + 2));

    printf("%lld\n", (long long)(total));
    return 0;
}
