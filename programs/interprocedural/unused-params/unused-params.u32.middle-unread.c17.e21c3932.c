// a unsigned int parameter nothing reads, middle-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int pick(unsigned int first, unsigned int ignored, unsigned int last) {
    return (unsigned int)(first * 10 + last);
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    total += pick(seed, (unsigned int)(seed + 7), (unsigned int)(seed + 1));
    total += pick(seed, seed, (unsigned int)(seed + 2));

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
