// a unsigned int parameter nothing reads, argument-has-a-side-effect
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int keep(unsigned int value, unsigned int ignored) {
    return (unsigned int)(value + 1);
}
static unsigned int counter = (unsigned int)0u;
static unsigned int bump(void) {
    counter = (unsigned int)(counter + 1);
    return counter;
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    total += keep(seed, bump());
    total += keep(seed, bump());

    printf("%llu\n", (unsigned long long)(counter));

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
