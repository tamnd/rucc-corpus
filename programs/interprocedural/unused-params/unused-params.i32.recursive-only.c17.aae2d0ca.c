// a int parameter nothing reads, recursive-only
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int walk(int value, int carried, int depth) {
    if (depth == (int)0) {
        return value;
    }
    return walk((int)(value + 1), carried, (int)(depth - 1));
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += walk(seed, (int)(seed * 4), (int)3);

    printf("%lld\n", (long long)(total));
    return 0;
}
