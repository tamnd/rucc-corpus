// a long long parameter nothing reads, all-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long fixed(long long one, long long two) {
    return (long long)7ll;
}
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    total += fixed(seed, (long long)(seed + 1));
    total += fixed(seed, (long long)(seed + 1));

    printf("%lld\n", (long long)(total));
    return 0;
}
