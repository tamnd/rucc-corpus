// a int parameter nothing reads, transitive-sum
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int keep(int value, int ignored) {
    return (int)(value + 1);
}
static int outer(int value, int left, int right) {
    return keep(value, (int)(left + right));
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += outer(seed, (int)(seed * 2), (int)(seed + 5));
    total += outer(seed, (int)(seed * 2), (int)(seed + 5));

    printf("%lld\n", (long long)(total));
    return 0;
}
