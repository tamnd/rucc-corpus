// a int parameter nothing reads, variadic
// Generated by rucc-corpus. Edit the generator, not this file.

#include <stdarg.h>

int printf(const char *, ...);

static int first(int value, ...) {
    return (int)(value + 1);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += first(seed, seed);
    total += first(seed, seed);

    printf("%lld\n", (long long)(total));
    return 0;
}
