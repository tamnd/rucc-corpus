// a long long parameter nothing reads, argument-has-a-side-effect
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long keep(long long value, long long ignored) {
    return (long long)(value + 1);
}
static long long counter = (long long)0ll;
static long long bump(void) {
    counter = (long long)(counter + 1);
    return counter;
}
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    total += keep(seed, bump());
    total += keep(seed, bump());

    printf("%lld\n", (long long)(counter));

    printf("%lld\n", (long long)(total));
    return 0;
}
