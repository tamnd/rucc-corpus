// a int parameter nothing reads, all-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int fixed(int one, int two) {
    return (int)7;
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += fixed(seed, (int)(seed + 1));
    total += fixed(seed, (int)(seed + 1));

    printf("%lld\n", (long long)(total));
    return 0;
}
