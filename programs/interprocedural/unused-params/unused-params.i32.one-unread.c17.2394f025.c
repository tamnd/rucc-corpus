// a int parameter nothing reads, one-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int keep(int value, int ignored) {
    return (int)(value + 1);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += keep(seed, (int)(seed * 5));
    total += keep(seed, (int)(seed * 5));

    printf("%lld\n", (long long)(total));
    return 0;
}
