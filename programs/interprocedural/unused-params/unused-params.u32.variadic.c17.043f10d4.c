// a unsigned int parameter nothing reads, variadic
// Generated by rucc-corpus. Edit the generator, not this file.

#include <stdarg.h>

int printf(const char *, ...);

static unsigned int first(unsigned int value, ...) {
    return (unsigned int)(value + 1);
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    total += first(seed, seed);
    total += first(seed, seed);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
