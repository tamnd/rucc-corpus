// a int parameter nothing reads, argument-is-an-address
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int note(int value, int *ignored) {
    return (int)(value + 1);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    int spot = (int)(seed * 3);
    total += note(seed, &spot);

    printf("%lld\n", (long long)(spot));

    printf("%lld\n", (long long)(total));
    return 0;
}
