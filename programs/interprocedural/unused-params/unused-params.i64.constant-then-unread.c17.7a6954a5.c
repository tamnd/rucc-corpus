// a long long parameter nothing reads, constant-then-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long scaled(long long value, long long factor) {
    return (long long)(value * factor);
}
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    total += scaled(seed, (long long)10ll);
    total += scaled(seed, (long long)10ll);

    printf("%lld\n", (long long)(total));
    return 0;
}
