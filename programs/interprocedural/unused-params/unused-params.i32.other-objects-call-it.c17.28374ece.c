// a int parameter nothing reads, other-objects-call-it
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int keep(int value, int ignored) {
    return (int)(value + 1);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += keep(seed, seed);
    total += keep(seed, seed);

    printf("%lld\n", (long long)(total));
    return 0;
}
