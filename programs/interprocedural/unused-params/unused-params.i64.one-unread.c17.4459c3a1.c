// a long long parameter nothing reads, one-unread
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long keep(long long value, long long ignored) {
    return (long long)(value + 1);
}
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    total += keep(seed, (long long)(seed * 5));
    total += keep(seed, (long long)(seed * 5));

    printf("%lld\n", (long long)(total));
    return 0;
}
