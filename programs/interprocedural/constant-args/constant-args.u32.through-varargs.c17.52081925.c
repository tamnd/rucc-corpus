// a unsigned int argument that is constant, through-varargs
// Generated by rucc-corpus. Edit the generator, not this file.

#include <stdarg.h>

int printf(const char *, ...);

static unsigned int pick(unsigned int value, ...) {
    va_list rest;
    unsigned int factor;
    va_start(rest, value);
    factor = va_arg(rest, unsigned int);
    va_end(rest);
    return value * factor;
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    total += pick(seed, (unsigned int)10u);
    total += pick(seed, (unsigned int)10u);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
