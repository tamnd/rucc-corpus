// a int argument that is constant, sites-disagree
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int scale(int value, int factor) {
    return value * factor;
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += scale(seed, (int)10);
    total += scale(seed, (int)10);
    total += scale(seed, (int)4);

    printf("%lld\n", (long long)(total));
    return 0;
}
