// a int argument that is constant, handed-on
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int scale(int value, int factor) {
    return value * factor;
}
static int outer(int value, int factor) {
    return scale(value, factor);
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += outer(seed, (int)10);
    total += outer(seed, (int)10);

    printf("%lld\n", (long long)(total));
    return 0;
}
