// a unsigned long long argument that is constant, through-varargs
// Generated by rucc-corpus. Edit the generator, not this file.

#include <stdarg.h>

int printf(const char *, ...);

static unsigned long long pick(unsigned long long value, ...) {
    va_list rest;
    unsigned long long factor;
    va_start(rest, value);
    factor = va_arg(rest, unsigned long long);
    va_end(rest);
    return value * factor;
}
static volatile unsigned long long seed_in = 3ull;

int main(void) {
    unsigned long long seed = seed_in;

    unsigned long long total = 0;
    total += pick(seed, (unsigned long long)10ull);
    total += pick(seed, (unsigned long long)10ull);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
