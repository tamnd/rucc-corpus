// a long long argument that is constant, through-varargs
// Generated by rucc-corpus. Edit the generator, not this file.

#include <stdarg.h>

int printf(const char *, ...);

static long long pick(long long value, ...) {
    va_list rest;
    long long factor;
    va_start(rest, value);
    factor = va_arg(rest, long long);
    va_end(rest);
    return value * factor;
}
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    total += pick(seed, (long long)10ll);
    total += pick(seed, (long long)10ll);

    printf("%lld\n", (long long)(total));
    return 0;
}
