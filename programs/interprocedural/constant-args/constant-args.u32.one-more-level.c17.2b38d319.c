// a unsigned int argument that is constant, one-more-level
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int scale(unsigned int value, unsigned int factor) {
    return value * factor;
}
static unsigned int outer(unsigned int value, unsigned int factor) {
    return scale(value, (unsigned int)(factor + 1));
}
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    total += outer(seed, (unsigned int)9u);
    total += outer(seed, (unsigned int)9u);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
