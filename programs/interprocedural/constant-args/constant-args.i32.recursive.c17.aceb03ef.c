// a int argument that is constant, recursive
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int down(int value, int factor, int depth) {
    if (depth == (int)0) {
        return value * factor;
    }
    return down(value, factor, (int)(depth - 1));
}
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    total += down(seed, (int)10, (int)2);
    total += down(seed, (int)10, (int)2);

    printf("%lld\n", (long long)(total));
    return 0;
}
