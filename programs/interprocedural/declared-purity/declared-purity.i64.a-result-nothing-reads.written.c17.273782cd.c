// a long long callee in another file, a result nothing reads, promise written
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

__attribute__((const)) long long corpus_weigh(long long of);
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    corpus_weigh(seed);
    total += corpus_weigh(seed);

    printf("%lld\n", (long long)(total));
    return 0;
}
