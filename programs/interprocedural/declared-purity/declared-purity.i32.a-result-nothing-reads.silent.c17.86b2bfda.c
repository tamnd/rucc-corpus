// a int callee in another file, a result nothing reads, promise silent
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int corpus_weigh(int of);
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    corpus_weigh(seed);
    total += corpus_weigh(seed);

    printf("%lld\n", (long long)(total));
    return 0;
}
