// a long long callee in another file, an array read twice, promise silent
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

long long corpus_look(const long long *at);
static volatile long long seed_in = 3ll;

int main(void) {
    long long seed = seed_in;

    long long total = 0;
    long long table[4] = {
        seed, (long long)(seed + 1), (long long)(seed + 2), (long long)(seed + 3)
    };
    total += corpus_look(table);
    total += corpus_look(table);

    printf("%lld\n", (long long)(total));
    return 0;
}
