// a int callee in another file, an array read twice, promise silent
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int corpus_look(const int *at);
static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int total = 0;
    int table[4] = {
        seed, (int)(seed + 1), (int)(seed + 2), (int)(seed + 3)
    };
    total += corpus_look(table);
    total += corpus_look(table);

    printf("%lld\n", (long long)(total));
    return 0;
}
