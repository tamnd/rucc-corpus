// a unsigned int callee in another file, the same argument twice, promise written
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

__attribute__((const)) unsigned int corpus_weigh(unsigned int of);
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    total += corpus_weigh(seed);
    total += corpus_weigh(seed);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
