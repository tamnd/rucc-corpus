// a unsigned int callee in another file, a write between two reads, promise written
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

__attribute__((pure)) unsigned int corpus_look(const unsigned int *at);
static volatile unsigned int seed_in = 3u;

int main(void) {
    unsigned int seed = seed_in;

    unsigned int total = 0;
    unsigned int table[4] = {
        seed, (unsigned int)(seed + 1), (unsigned int)(seed + 2), (unsigned int)(seed + 3)
    };
    total += corpus_look(table);
    table[2] = (unsigned int)(seed + 10);
    total += corpus_look(table);

    printf("%llu\n", (unsigned long long)(total));
    return 0;
}
