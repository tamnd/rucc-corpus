// mutual tail recursion 5000 deep accumulating into unsigned long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned long long odd(int left, unsigned long long total);
static unsigned long long even(int left, unsigned long long total) {
    if (left == 0) {
        return total;
    }
    return odd(left - 1, total + 1);
}
static unsigned long long odd(int left, unsigned long long total) {
    if (left == 0) {
        return total;
    }
    return even(left - 1, total + 1);
}
static unsigned long long walk(int left, unsigned long long total) {
    return even(left, total);
}
static volatile int depth_in = 5000;

int main(void) {
    int depth = depth_in;

    printf("%llu\n", (unsigned long long)(walk(depth, (unsigned long long)0ull)));
    return 0;
}
