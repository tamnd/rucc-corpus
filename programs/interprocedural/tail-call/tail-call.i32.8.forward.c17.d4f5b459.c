// forward tail recursion 8 deep accumulating into int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int finish(int total) {
    return total;
}
static int step(int left, int total) {
    if (left == 0) {
        return finish(total);
    }
    return step(left - 1, total + 1);
}
static int walk(int left, int total) {
    return step(left, total);
}
static volatile int depth_in = 8;

int main(void) {
    int depth = depth_in;

    printf("%lld\n", (long long)(walk(depth, (int)0)));
    return 0;
}
