// mutual tail recursion 8 deep accumulating into int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int odd(int left, int total);
static int even(int left, int total) {
    if (left == 0) {
        return total;
    }
    return odd(left - 1, total + 1);
}
static int odd(int left, int total) {
    if (left == 0) {
        return total;
    }
    return even(left - 1, total + 1);
}
static int walk(int left, int total) {
    return even(left, total);
}
static volatile int depth_in = 8;

int main(void) {
    int depth = depth_in;

    printf("%lld\n", (long long)(walk(depth, (int)0)));
    return 0;
}
