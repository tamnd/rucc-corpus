// forward tail recursion 5000 deep accumulating into unsigned int
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static unsigned int finish(unsigned int total) {
    return total;
}
static unsigned int step(int left, unsigned int total) {
    if (left == 0) {
        return finish(total);
    }
    return step(left - 1, total + 1);
}
static unsigned int walk(int left, unsigned int total) {
    return step(left, total);
}
static volatile int depth_in = 5000;

int main(void) {
    int depth = depth_in;

    printf("%llu\n", (unsigned long long)(walk(depth, (unsigned int)0u)));
    return 0;
}
