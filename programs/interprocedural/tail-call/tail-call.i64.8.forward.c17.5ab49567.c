// forward tail recursion 8 deep accumulating into long long
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static long long finish(long long total) {
    return total;
}
static long long step(int left, long long total) {
    if (left == 0) {
        return finish(total);
    }
    return step(left - 1, total + 1);
}
static long long walk(int left, long long total) {
    return step(left, total);
}
static volatile int depth_in = 8;

int main(void) {
    int depth = depth_in;

    printf("%lld\n", (long long)(walk(depth, (long long)0ll)));
    return 0;
}
