// a wide-if-chain shape, which the analyses have to fit
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 5;

int main(void) {
    int seed = seed_in;

    int total = 0;
    if (seed > 0) total += 1;
    if (seed > 1) total += 2;
    if (seed > 2) total += 3;
    if (seed > 3) total += 4;
    if (seed > 4) total += 5;
    if (seed > 5) total += 6;
    if (seed > 6) total += 7;
    if (seed > 7) total += 8;
    if (seed > 8) total += 9;
    if (seed > 9) total += 10;
    if (seed > 10) total += 11;
    if (seed > 11) total += 12;
    if (seed > 12) total += 13;
    if (seed > 13) total += 14;
    if (seed > 14) total += 15;
    if (seed > 15) total += 16;

    printf("%lld\n", (long long)(total));
    return 0;
}
