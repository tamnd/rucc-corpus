// a jumped-over-block shape, which the analyses have to fit
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 4;

int main(void) {
    int seed = seed_in;

    int total = seed;
    goto past;
    total = total * 100;
    past:
        total += 1;

    printf("%lld\n", (long long)(total));
    return 0;
}
