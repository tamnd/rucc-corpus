// a continue-and-break shape, which the analyses have to fit
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 2;

int main(void) {
    int seed = seed_in;

    int total = 0;
    for (int i = 0; i < 6; i++) {
        if (i % 2) continue;
        for (int j = 0; j < 6; j++) {
            if (j > i) break;
            total += seed;
        }
        if (total > 100) break;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
