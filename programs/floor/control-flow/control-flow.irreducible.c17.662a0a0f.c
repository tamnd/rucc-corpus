// a irreducible shape, which the analyses have to fit
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int enter_second_in = 1;

int main(void) {
    int enter_second = enter_second_in;

    int total = 0;
    if (enter_second) goto second;
    first:
        total += 1;
        if (total > 20) goto out;
        goto second;
    second:
        total += 2;
        if (total > 20) goto out;
        goto first;
    out:
        ;

    printf("%lld\n", (long long)(total));
    return 0;
}
