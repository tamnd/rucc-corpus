// a two-exit-loop shape, which the analyses have to fit
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int limit_in = 6;

int main(void) {
    int limit = limit_in;

    int total = 0;
    int hit = 0;
    for (int i = 0; i < 10; i++) {
        if (i == limit) { hit = 1; break; }
        total += i;
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(hit));
    return 0;
}
