// a int branch the static predictors call arm-with-a-call, taken as predicted
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int slow(int v) {
    return v;
}
static volatile int flag_in = 0;

int main(void) {
    int flag = flag_in;

    int total;
    if (flag) {
        total = slow(7);
    } else {
        total = 7;
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
