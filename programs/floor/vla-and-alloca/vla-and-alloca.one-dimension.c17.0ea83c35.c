// a run time sized object, one-dimension
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int count_in = 6;

int main(void) {
    int count = count_in;

    int values[count];
    for (int i = 0; i < count; i++) {
        values[i] = i * i;
    }
    int total = 0;
    for (int i = 0; i < count; i++) {
        total = total + values[i];
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(values[0] + values[count - 1]));
    return 0;
}
