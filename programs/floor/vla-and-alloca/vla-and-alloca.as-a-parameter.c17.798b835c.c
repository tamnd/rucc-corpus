// a run time sized object, as-a-parameter
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static int corner(int rows, int cols, int grid[rows][cols]) {
    return grid[rows - 1][cols - 1];
}
static int total(int rows, int cols, int grid[rows][cols]) {
    int sum = 0;
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            sum = sum + grid[i][j];
        }
    }
    return sum;
}
static volatile int rows_in = 3;
static volatile int cols_in = 4;

int main(void) {
    int rows = rows_in;
    int cols = cols_in;

    int grid[rows][cols];
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            grid[i][j] = i * 10 + j;
        }
    }

    printf("%lld\n", (long long)(total(rows, cols, grid)));
    printf("%lld\n", (long long)(corner(rows, cols, grid)));
    return 0;
}
