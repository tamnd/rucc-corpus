// a run time sized object, both-in-one-frame
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int count_in = 5;

int main(void) {
    int count = count_in;

    int values[count];
    unsigned char *room = (unsigned char *)__builtin_alloca(count);
    for (int i = 0; i < count; i++) {
        values[i] = i + 1;
        room[i] = (unsigned char)(i * 2);
    }
    int from_array = 0;
    int from_alloca = 0;
    for (int i = 0; i < count; i++) {
        from_array = from_array + values[i];
        from_alloca = from_alloca + room[i];
    }

    printf("%lld\n", (long long)(from_array));
    printf("%lld\n", (long long)(from_alloca));
    printf("%lld\n", (long long)(from_array + from_alloca));
    return 0;
}
