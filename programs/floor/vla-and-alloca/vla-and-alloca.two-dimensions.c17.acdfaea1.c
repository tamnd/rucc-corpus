// a run time sized object, two-dimensions
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int rows_in = 3;
static volatile int cols_in = 4;

int main(void) {
    int rows = rows_in;
    int cols = cols_in;

    int grid[rows][cols];
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            grid[i][j] = i * 10 + j;
        }
    }
    int total = 0;
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            total = total + grid[i][j];
        }
    }

    printf("%lld\n", (long long)(total));
    printf("%lld\n", (long long)(grid[2][3]));
    printf("%lld\n", (long long)(grid[0][0]));
    return 0;
}
