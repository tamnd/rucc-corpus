// a run time sized object, pointer-to-a-row
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int rows_in = 3;
static volatile int cols_in = 4;

int main(void) {
    int rows = rows_in;
    int cols = cols_in;

    int grid[rows][cols];
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            grid[i][j] = i * 10 + j;
        }
    }
    int (*row)[cols] = grid;
    int walked = 0;
    for (int i = 0; i < rows; i++) {
        walked = walked + (*row)[i];
        row = row + 1;
    }

    printf("%lld\n", (long long)(walked));
    return 0;
}
