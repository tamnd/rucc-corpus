// a run time sized object, size-changes-each-time
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int base_in = 2;

int main(void) {
    int base = base_in;

    int total = 0;
    for (int round = 0; round < 5; round++) {
        int room = base + round;
        int values[room];
        for (int j = 0; j < room; j++) {
            values[j] = j + 1;
        }
        total = total + values[room - 1];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
