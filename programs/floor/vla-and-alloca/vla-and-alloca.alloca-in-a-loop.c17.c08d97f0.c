// a run time sized object, alloca-in-a-loop
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int rounds_in = 4;

int main(void) {
    int rounds = rounds_in;

    int total = 0;
    for (int i = 0; i < rounds; i++) {
        unsigned char *room = (unsigned char *)__builtin_alloca(i + 1);
        room[i] = (unsigned char)(i + 1);
        total = total + room[i];
    }

    printf("%lld\n", (long long)(total));
    return 0;
}
