// locals, assignment and sequencing
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 3;

int main(void) {
    int seed = seed_in;

    int one = seed;
    int two = one + one;
    int three = two * two;
    one = three - two;

    printf("%lld\n", (long long)(one));
    printf("%lld\n", (long long)(two));
    printf("%lld\n", (long long)(three));
    return 0;
}
