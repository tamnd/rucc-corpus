// a bubble sort, which uses most of the above at once
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static void sort(int *values, int count) {
    for (int pass = 0; pass < count; pass++) {
        for (int at = 0; at + 1 < count; at++) {
            if (values[at] > values[at + 1]) {
                int held = values[at];
                values[at] = values[at + 1];
                values[at + 1] = held;
            }
        }
    }
}
static volatile int seed_in = 7;

int main(void) {
    int seed = seed_in;

    int values[8];
    for (int i = 0; i < 8; i++) {
        values[i] = (i * seed) % 8;
    }
    sort(values, 8);

    printf("%lld\n", (long long)(values[0]));
    printf("%lld\n", (long long)(values[1]));
    printf("%lld\n", (long long)(values[2]));
    printf("%lld\n", (long long)(values[3]));
    printf("%lld\n", (long long)(values[4]));
    printf("%lld\n", (long long)(values[5]));
    printf("%lld\n", (long long)(values[6]));
    printf("%lld\n", (long long)(values[7]));
    return 0;
}
