// indexing, and walking an array with a pointer
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

int main(void) {
    int values[8];
    for (int i = 0; i < 8; i++) {
        values[i] = i * i;
    }
    int total = 0;
    for (int *at = values; at != values + 8; at++) {
        total += *at;
    }

    printf("%lld\n", (long long)(values[0]));
    printf("%lld\n", (long long)(values[7]));
    printf("%lld\n", (long long)(total));
    return 0;
}
