// the comma-and-conditional corner of C17
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

static volatile int seed_in = 4;

int main(void) {
    int seed = seed_in;

    int stepped = 0;
    int value = (stepped = seed, stepped + 1);

    printf("%lld\n", (long long)(value));
    printf("%lld\n", (long long)(stepped));
    printf("%lld\n", (long long)(seed > 0 ? seed < 10 ? 1 : 2 : 3));
    return 0;
}
