// the anonymous-members corner of C17
// Generated by rucc-corpus. Edit the generator, not this file.

int printf(const char *, ...);

struct outer { int tag; union { int as_int; unsigned int as_bits; }; };

int main(void) {

    struct outer held;
    held.tag = 1;
    held.as_int = 12;

    printf("%lld\n", (long long)(held.tag));
    printf("%lld\n", (long long)((int)held.as_bits));
    return 0;
}
